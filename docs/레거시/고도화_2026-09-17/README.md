# CUVIA NDMS Phase 2 고도화 마스터 플랜

> 상태: 대표 데모 상세 설계 정리 완료·I0 구현 착수 전 - 실제 자료 확인은 후속 작업과 병행
>
> 기준일: 2026-09-11
>
> 역할: Phase 2의 목표, 조사 기준, 산출물, 작업 순서와 의사결정 상태를 관리하는 상위 계획 문서

## 1. 이 문서의 역할

이 README는 상세 요구사항을 모두 담는 문서가 아니다. Phase 2에서 무엇을 왜 조사하고, 어떤 순서로
설계·구현하며, 무엇이 결정됐고 무엇이 남았는지를 관리하는 **마스터 플랜**이다.

세부 내용은 이 계획에 따라 별도 문서에서 작성한다. 같은 내용을 여러 문서에 반복하지 않으며,
README에는 확정된 방향, 문서 간 관계, 진행 상태와 결정 관문만 유지한다.

## 2. Phase 2의 목적

Phase 2는 현재 구현을 정답으로 두고 기능과 재난유형을 계속 추가하는 작업이 아니다.

> **원본 자료와 추가 조사 결과를 기준으로 CUVIA의 제품 방향을 새로 설계하고, 여러 이벤트를 하나의
> 재난 사건으로 해석해 디지털트윈 예측과 대응에 활용하는 방식을 대표 데모로 증명한다.**

이 데모는 한 가지 목적만 가진 시제품이 아니다. 영업 현장에서는 CUVIA가 지향하는 제품 경험과 차별점을
설득력 있게 보여주고, 내부에서는 기획·디자인 의도를 검증하며, 이후 개발자가 같은 코드와 정보구조 위에
실제 API·모델·업무 연계를 교체·확장할 수 있는 **비전 데모이자 실개발 출발점**이다. 따라서 운영 제품의
모든 기능을 완성하는 것이 목표는 아니지만, 화면과 상호작용은 실제 제품으로 이어질 수 있게 설계한다.

이번 단계에서 답해야 할 핵심 질문은 다음과 같다.

1. CUVIA는 어떤 원천 데이터와 이벤트를 수집하는가?
2. 서로 다른 이벤트를 어떤 기준으로 하나의 재난 사건에 연결하는가?
3. 사건 유형에 따라 디지털트윈의 입력, 시간축, 공간 표현과 결과가 어떻게 달라지는가?
4. 예측 결과가 담당자의 판단, SOP, 전파, 조치와 기록으로 어떻게 이어지는가?
5. 이 방향을 한 건의 대표 사건으로 어떻게 이해하기 쉽게 시연할 것인가?

### 2.1 Phase 2를 끌고 갈 핵심 제품 흐름

개별 기능의 수가 아니라 다음 흐름이 CUVIA Phase 2의 중심이다.

```text
센서·시설·공공/기관 API·CCTV/VLM
    ↓ 수집·정규화
원천·파생·분석 이벤트
    ↓ 시간·공간·의미·품질 결합
위험도 매트릭스 판단 + 디지털트윈 예측 영향
    ↓ 위험등급·기여 근거·예상 영향
복합 알림과 사건 후보
    ↓ 담당자 확인
사건 확정·현재 판단·기준/대안 전망
    ↓
SOP 권고·승인·전파·조치·결과 확인
    ↓
사건 타임라인·보고서 자동 생성·예측 검증
```

센서 임계치, API 특보, VLM 분석과 디지털트윈 예측은 각각 독립된 근거 이벤트다. 위험도 매트릭스는
수위·강우·영상 맥락과 시설·예측·품질 정보를 다지표로 결합해 위험등급과 지표별 기여 근거를 계산한다.
시스템은 매트릭스 기준 또는 사전에 정한 단일 심각·복합 조건이 충족되면 담당자가 확인할 복합 알림과
사건 후보를 자동 생성할 수 있다. 사건 확정, 대외 전파, 시설 제어와 도시 대응단계 변경은 담당자·승인자의
결정으로 남긴다.

`이벤트 발생`, `담당자 알림`, `사건 후보 생성`, `도시 대응단계 변경`은 같은 상태가 아니다. 알림은
이벤트·Forecast의 조합을 사람이 검토할 업무 신호로 바꾼 것이며, 사건 후보는 그 근거를 묶어 처리상태와
담당을 갖게 한 Incident다. 이 구분과 전체 흐름이 개별 화면·기능보다 우선한다.

### 2.2 차별점 기준 — 예측에서 끝나지 않고, 대응 이후의 미래까지 비교한다 (2026-09-16 사용자)

> **재난이 발생하기 전에 미래 영향을 예측하고, 대응을 바꿨을 때 달라지는 결과까지 비교한다.**

화면·기능·문구를 새로 넣거나 뺄 때는 이 문장의 어느 단계를 강하게 하는지로 판단한다. 흐름에 기여하지 않는 연출은 우선하지 않는다.

**차별점이 아닌 것.** `디지털트윈`이라는 단어, 3D 지도, 단순 미래예측은 차별점이 아니다. 경쟁 자료(XEUS · 복합재난감시)도 실시간 관측·GIS·예측·
이벤트 분석·상황전파·SOP까지는 이미 말하고, 디지털트윈을 시뮬레이션·상황예측에 쓴다고 하며, 피해예측·확산예측을 내세운다. 다만 그들 자료에서
`예측`과 `대응`은 각각의 기능으로 나열된다.

**차별점인 것.** 그 둘을 하나의 판단 흐름으로 잇는 것이다. 재난이 터진 뒤 보는 관제가 아니라, 피해가 예상되는 순간 사건이 먼저 생기고, 그 사건의
미래를 보고, 대응 조건을 바꿔 결과 차이까지 확인한 뒤 판단한다. 사건이 끝나면 실제 결과로 예측을 검증한다.

```text
관측·예측
  ↓ 피해 발생 전 예측 이벤트
사건 생성
  ↓
전망            "지금대로면 어디가 언제 얼마나 위험해지나?"      디지털트윈 기준 시뮬레이션
  ↓
대안 분석        "수문을 20분 먼저 열면? 배수를 지금 강화하면?"   전망 탭 대안 비교(진행 중) · 모의훈련(지난 사건)
  ↓ 미래 영향 변화 비교
대응 판단 → 실행
  ↓
사후 검증        "당시 예측이 실제로 얼마나 맞았나?"              기록·검증 예측 케이스
```

- **전망과 대안 분석은 둘 다 디지털트윈의 결과다.** 전망은 "그대로면?", 대안 분석은 그 전망을 기준으로 "바꾸면?"이다. 업무 단계가 달라 화면에서 나눠 보여줄 뿐,
  별개의 세계가 아니다. 진행 중 사건의 대안 비교는 사건 작업공간 전망 탭이, 지난 사건에서 조건·대응을 바꿔 돌려 보는 일은 모의훈련(§2.3)이 맡는다.
  결과 값의 근거는 §2.3 기준 ③을 따른다.
- **우선순위.** ① 피해 전에 사건을 띄우는 예측 ② 시간·공간으로 미래 영향을 보여주는 전망 ③ 대응 시점·수준·범위를 바꾼 대안 미래(여기서 차별점이 선다)
  ④ 실제 결과와 검증(여기서 제품 완성도가 오른다).
- **신뢰 조건.** 대안 결과는 믿을 만해야 차별점이 된다. 제품의 도달점은 `사건 입력값 + 대응 파라미터 → 모델 또는 명시 규칙 → 대안 Forecast`다.
  Phase 2 데모는 조정값 선택지를 고정하고 조합마다 사전 작성한 대안 예측판을 쓴다(2026-09-16 결정). 그래서 대안 결과를 실시간 물리 계산처럼 부르지 않고,
  실제 모델이 연결되면 같은 계약의 ModelRun 결과로 교체한다.
- **대외 문장.** 긴 문장은 위 인용, 짧은 문장은 "예측에서 끝나지 않고, 대응 이후의 미래까지 비교"다. `디지털트윈`을 앞세우지 않는다.

### 2.3 모의훈련 기준 (2026-09-16 사용자)

> **이 조건에서 우리 규정이 통하는지 확인하고, 안 통하는 지점을 고칠 거리로 남긴다.**

**목적을 이렇게 좁힌 이유.** 공식 근거가 말하는 훈련의 목적은 셋이다. 역량 숙지(재난안전법 제35조 "대응 역량 강화"),
계획 검증, 갭 도출(HSEEP: validate plans · AAR/IP). 이 중 **역량 숙지는 화면이 대신할 수 없다** — 사람을 모아
시간 압박 속에서 시키는 일이고 훈련 운영의 몫이다. 우리가 남보다 잘할 수 있는 것은 계획 검증과 갭 도출이다.
예측 판이 있어 "규정대로 했으면 결과가 어땠나"를 계산으로 보일 수 있고, 그 자리는 타사 7건에 없다.

이 목적이 아래 결정들의 이유다. 점수를 매기지 않는 것(역량 평가가 목적이 아니라서), 산출물이 개선 항목과
보고서인 것(갭 도출이 목적이라서), 조건 단계를 문턱에만 두는 것(규정이 어디까지 통하는지 보려는 것이라서),
결과 경로가 없는 항목을 넣지 않는 것(검증할 방법이 없어서)이 모두 여기서 나온다.

**자리.** `/scr-05`는 `모의훈련` 메뉴다(창원 제안서 p.15·16 "모의훈련 E-SOP 관리"). 진행 중 사건의 대안 비교는 사건 작업공간의
전망 탭이 맡는다. 대응 판단과 조치안이 그 화면에 있기 때문이다. 이 기준은 2026-09-16 방향 검수(타사 7건·사업 원본 5건 원문 대조)에서 나왔다.

**진행 중 사건은 모의훈련에서 `예측 근거`만 연다(2026-09-17 확정).** 훈련은 못 한다 — 훈련의 기준은
"실제와 같게 했을 때"인데 그 실제가 아직 없다. 대신 **지금 서 있는 전망이 무엇으로 계산됐는지**는 여기서 재 본다:
모델·기준시각·계산 주체·입력 품질 / 무엇을 언제까지 받았나 / 무엇을 가정했나 / 어디에 민감하고 어느 구간은 못 쓰나(`ForecastBasis`).
**예측을 믿고 대응을 정하는 자리와, 그 예측이 믿을 만한지 재는 자리는 다르다.** 앞은 재난관제, 뒤가 모의훈련이다.
근거 창은 등급을 매기거나 "믿을 만하다/아니다"를 말하지 않는다. 무엇이 들어갔는지를 보이고 판단은 보는 사람이 한다.
대응 판단으로 건너가는 링크(`재난관제에서 열기`)를 창 안에 둔다 — 경계는 그대로다.

**기준 ① 업계 기본은 그대로 가져온다.**

| 기능 | 근거 | 화면의 어디에 (2026-09-16 v2) |
|---|---|---|
| SOP 시나리오 구성·템플릿 | RINO 요약제품소개서 p.4·5 | 준비 화면의 훈련 구조 · 판단 국면의 조치 카드 |
| 단계별 응대 지침·조치 결과 입력 | XEUS SIREN 제안자료 p.22 | 조치 카드의 지침 줄과 [실행] · 내 조치 기록 |
| 대응 이력 자동 저장·리포트 | XEUS SIREN 제안자료 p.22 | 강평 · 훈련 보고서 · 지난 훈련 목록 |
| 과거 이력·유사 사례 조회 | 복합재난감시 제안장표 p.6, AI 재난안전 통합플랫폼 p.11 | **Phase 2에서 뺀다** · 유사 판단 기준이 없다 |

**기준 ② CUVIA 장점은 근거가 있는 것만 둔다.**

1. 모의훈련 자체. 자사 경쟁 비교표에서 CUVIA ◎, 경쟁 5사 X다(요약설명자료 슬3). 타사 7건에 훈련 기능이 없다. → 화면 전체
2. 실제 사건이 훈련 시나리오가 된다. 제안서 p.16 "과거재난이력으로 가상재난 발생 시나리오를 자동 생성"을 사건 원장이 받친다. → 목록·준비 화면
3. 단일 재난 시나리오를 조합한 복합 재난 시나리오(제안서 p.15). → 준비 화면의 조건(강우 단계 · 만조·펌프는 확장)
4. 트윈 위에서 시나리오가 전개된다. 수치는 기준 ③을 넘는 것만 선다(24년 도시침수 완료보고 p.39·40 조건별 시나리오 DB가 대표 경로).
   → **판단·결과 국면. 지도가 정지점마다 바뀐다.** v1에 없던 자리이고 우리만의 것이다
5. 결과가 개선 항목으로 이어진다. 타사 7건에는 이 고리가 없다. → 강평의 고칠 거리

**기준 ③ 결과 값은 두 시험을 통과해야 선다.**

- **경로:** 그 값을 실제로 낼 경로가 있다. 경로는 모델 입력(실증 모델 또는 외부 모델 수신), 공식 기준·규칙, 사건 원장 중 하나다.
  데모 값은 같은 계약의 사전 작성 판이고 계산 주체와 교체 대상을 내부에 적는다(§2.2 신뢰 조건). 경로가 확인되지 않은 유형은
  03 §15에 `확인 필요`로 둔다.
- **트윈 결과:** 트윈이 계산하는 도달·범위·강도·파급이다. 입력에 비례해 줄어드는 산수(일찍 할수록 그만큼 줄어드는 인원·차량·건물 수)는
  결과가 아니다. 데이터가 붙어도 산수는 트윈 결과가 되지 않는다.

하나라도 못 넘으면 수치로 만들지 않고 상황 부여(그 시각에 들어오는 상황 한 줄)로 둔다. 쉼터를 일찍 열면 취약 인원이 줄어든다거나,
통제로 진입 차량·노출 인원이 줄어든다거나, 영향 건물 동 수가 줄어든다는 값은 세우지 않는다. 노출 대응(통제·대피)의 결과는 도달 전
여유 시간 하나다. 창원천 방류 시점을 바꾼 수위·도달(저수지 유입·방류량이 모델 입력, 24년 보고서 p.39)과 강우·조위·펌프 가동률 조건을
바꾼 침수심·범위·시간(p.39·40)은 세운다.

**흐름.**

```text
센서 임계 → SOP 발동 → 실제 조치(지연) → 다르게 했다면 → 결과 → 판정 → 개선 항목 → 사람 검토·승인
```

**훈련 규칙 여섯 (2026-09-16 사용자와 정리).**

1. **대응은 SOP에서만 나온다.** SOP는 센서 임계에서 발동한다. 대응 목록을 사건마다 손으로 적지 않는다.
2. **훈련 지표는 하나다.** 발동에서 조치까지 걸린 시간. 창원천이면 14:35 예측 → 15:22 통제 = 47분.
   "10분 일찍" 같은 임의 눈금은 쓰지 않는다 — 왜 10분인지 답할 수 없다.
3. **대안 시각은 셋 중 하나다.** 알린 직후(이론상 최선 · 준비 시간은 확인 전까지 0으로 두고 상한이라고 적는다) ·
   문턱 앞뒤(결과가 갈리는 경계) · 실제.
4. **결과는 경로가 있는 것만 세운다.** 없으면 시간(도달 전 여유 · 지속 시간)만, 그것도 없으면 상황 부여(수치 없음).
5. **조건 단계는 규정이 버티는 한계를 드러내는 문턱에만 둔다.** 세기 눈금을 늘리지 않는다.
6. **결과 경로가 없는 항목은 훈련에 넣지 않는다.** 주민 전파가 그 예다 — 대피율·부상은 우리 데이터에 없고,
   채널 성공·실패는 판단 훈련이 아니라 장비 점검이다. 전파 지연은 지표로 쓸 수 있으나 다른 항목과 성격이 같아
   Phase 2에서 제외한다(2026-09-16 사용자 "채널별 성공 여부가 훈련에 필요하냐").

**판정 세 줄.** 결과 표 아래에 선다. 문장은 시스템이 짓지 않고 재료만 붙인다.

```text
지켰나      S3 47분 걸림 (S1은 즉시)
부족했나    강우 +50% 조건에서는 알리자마자 통제해도 도달 뒤
더 하면     실제 조건에서 14:35 통제 시 도달 47분 전 · 통제 지속 69분(실제 20분)
```

**학습의 뜻.** 사람·SOP·판단 기준을 고치는 것이다. 시뮬레이션 결과로 예측 모델을 학습시키지 않는다. 자기 출력을 되먹는 순환이 되기
때문이다. 모델 개선은 예측 검증(실측 비교)으로만 한다.

**개선 항목은 한 목록이다.** 예측 검증의 개선 항목(임계치·모델·데이터·SOP)에 출처 `훈련`을 더해 모은다. 훈련에서 나온 항목은 모델 축을
쓰지 않는다. 자동 반영은 없고 승인 뒤에 반영한다(§5 사람 승인).

**비전과 Phase 2는 따로 적는다.** 비전에는 시나리오 편집기, 복합 조합, 실행(실행·정지·종료·예약), 단계별 목표 시간(제안서 p.16),
훈련 후 개선 보고가 들어간다. Phase 2 범위는 §7에서 따로 정한다. Phase 2에서 빼는 항목도 비전에서 지우지 않는다.

## 3. 설계 출발점

### 3.1 새로 설계한다

- 현재 `designs/src`는 Phase 1 프로토타입이며 Phase 2 목표 사양이 아니다.
- 기존 화면과 코드는 재사용 가능성을 판단하기 위한 현황 자료로만 사용한다.
- 기존 구현의 메뉴, 화면 번호, 데이터 구조와 시나리오를 그대로 유지하는 것을 목표로 삼지 않는다.
- 원본 자료와 추가 조사에서 확인된 문제, 사용자 업무, 데이터와 기술 근거를 먼저 정리한 뒤 구조를 설계한다.
- 조사 결과 기존 방향이 적절하지 않으면 화면 내부뿐 아니라 IA와 업무 흐름도 재편할 수 있다.

### 3.2 백업 문서의 위치

`docs/레거시/고도화_백업/`은 삭제 전 자료를 보존하기 위한 **복구용 사본**이다.
`docs/레거시/작업/`은 정본 반영이 끝난 초안과 과거 작업 문서를 보존하는 **작업 이력**이다.

- 새 설계의 출발점이나 근거 문서로 사용하지 않는다.
- 백업 문서의 분류, 수량, 결론과 표현을 새 문서에 자동 승계하지 않는다.
- 원본에서 다시 확인할 수 없는 내용은 근거로 채택하지 않는다.
- 사용자가 별도로 요청하지 않는 한 백업 문서를 검토 대상으로 포함하지 않는다.
- `레거시/작업/`의 문서는 작성 당시 판단을 보존하므로 현재 정본과 충돌할 수 있다. 새 설계의 근거나
  활성 요구사항으로 인용하지 않고, 필요할 때 변경 이력 확인에만 사용한다.

## 4. 근거 자료 원칙

### 4.1 근거 우선순위

설계 판단은 다음 순서로 근거를 확인한다.

1. **사업·실증 원본:** 발주처 자료, 완료보고서, 실제 연계 명세와 운영 자료
2. **공식 표준·공공 근거:** 정부 정책, 국제 표준, 공공기관 기술·운영 지침
3. **자사 원본:** CUVIA 제품 소개서와 제안 자료
4. **타사·협력사 원본:** 경쟁 제품과 협력 범위 파악을 위한 자료
5. **현재 구현:** Phase 1이 실제로 보여주는 범위와 재사용 가능성 확인

제안서에 적힌 기능, 회사가 보유한 기술, 파트너가 제공할 기능, 현재 데모에 구현된 기능과 Phase 2에서
새로 만들 기능을 문서와 대외 설명에서 같은 상태로 취급하지 않는다. 이 구분은 기획 문서·데모 데이터
구조·필요 시 대외 설명에서 관리한다. 데모 화면은 목표 제품의 사용자 경험으로 만들며 `가상`, `더미`,
`계산 결과 아님` 같은 제작 사정이나 면책 문구를 기본 UI에 노출하지 않는다.

### 4.2 현재 확보된 원본

| 구분 | 자료 | 확인 목적 |
|---|---|---|
| 사업·실증 | `24년도시침수 완료보고_20250325_v1.0.pdf` | 창원 데이터, 연계 범위, 예측 실증과 한계 |
| 자사 | `지능형재난안전솔루션(쿠비아)_요약설명자료_v0.2_20260908.pptx` | CUVIA가 제시한 제품 비전과 데모 메시지 |
| 자사 | `창원시_지능형재난안전솔루션(쿠비아)_REV 1.0.pdf` | 제안 기능, 데이터와 업무 범위 |
| 타사 | `타사레퍼런스/` 지오멕스 자료 3종 | 업계 기본 기능, 사건관제·SOP·영상·GIS 표현 |
| 타사 | RINO 제품소개서 | 이벤트 규칙, 메시지 라우팅과 워크플로우 |
| 협력사 | TwinRIS-ON 솔루션 제안서 | 계측 통합, NDMS 연계와 3D 표현의 역할 경계 |

원본 파일은 `docs/참고자료/`에서 관리한다.

### 4.3 추가 조사 기준

추가 조사는 단순히 경쟁사 화면을 더 모으는 작업이 아니다. 아래 다섯 축에서 공식 자료와 실제 적용
사례를 찾아 제품 구조의 근거를 보강한다.

| 조사 축 | 확인할 내용 | 설계에 반영할 대상 |
|---|---|---|
| 이벤트 수집 | 센서·영상·기상·시설·기관 데이터의 식별자, 위치, 시각, 품질과 갱신 | 공통 관측 이벤트 모델 |
| 사건 전환 | 중복 제거, 시간·공간 연결, 원인·파생 관계, 사람 확인 | 사건 생성·병합·종료 규칙 |
| 예측·트윈 | 현재 상태, 기준 전망, 대안 조건, 영향 범위와 불확실성 | 유형별 디지털트윈 계약 |
| 대응·전파 | 심각도·긴급도·확실성, 승인, SOP, 채널별 결과 | 대응 실행과 전파 모델 |
| 기록·검증 | 입력 근거, 판단, 예측 버전, 실제 결과와 오차 | 타임라인·보고서·사후 검증 |

현재 상세 설계에 채택한 공식 출발점은 다음과 같다.

- [CloudEvents](https://github.com/cloudevents/spec/blob/main/cloudevents/spec.md): 출처가 다른 이벤트의
  공통 식별·출처·유형·시각·대상 봉투
- [OGC SensorThings API](https://www.ogc.org/standards/sensorthings/): 서로 다른 IoT 시스템의 장비,
  센서, 관측값과 위치를 일관된 구조로 다루기 위한 기준
- [OGC API EDR](https://docs.ogc.org/is/19-086r6/19-086r6.html): 위치·영역·경로·시간 기반 환경 데이터 조회
- [ETSI NGSI-LD](https://cim.etsi.org/NGSI-LD/official/introduction.html): 시설·구역·사건의 속성·관계·시간 변화
- [OASIS Common Alerting Protocol 1.2](https://www.oasis-open.org/standard/cap/): 경보의 식별자,
  심각도, 긴급도, 확실성, 대상 지역, 갱신과 취소를 표현하는 기준
- [ISO 22320:2018](https://www.iso.org/standard/67851.html): 역할·책임·업무·자원과 기관 간 공동 대응
- [WMO Early Warnings for All](https://wmo.int/all-activities/build-resilience/early-warnings-all):
  위험지식에서 관측·예측·전파·대응까지 이어지는 종단 흐름
- [NIST AI RMF 1.0](https://www.nist.gov/publications/artificial-intelligence-risk-management-framework-ai-rmf-10):
  AI 결과의 신뢰성·설명가능성·책임성과 사람의 확인·승인 경계

추가 자료를 채택할 때는 발행기관, 문서명, 버전 또는 기준일, 적용한 결정과 한계를 함께 기록한다.
검색 결과나 홍보 문구만으로 제품 기능을 확정하지 않는다.

## 5. 설계 원칙

- **사건 중심:** 센서나 기능 목록보다 담당자가 판단하고 대응할 사건이 중심이다.
- **원천과 판단 분리:** 수집값, 관측 이벤트, 시스템 판단, 담당자 확인을 구분한다.
- **현재와 미래 분리:** 현재 관측, 조건 유지 전망, 대안 전망과 실제 결과를 구분한다.
- **공통 골격과 유형별 차이:** 업무 흐름은 공통으로 두고 트윈의 입력·표현·결과는 유형별로 설계한다.
- **사람 승인:** AI와 예측은 근거와 권고를 제공하며 되돌리기 어려운 조치는 담당자가 확정한다.
- **근거 추적:** 화면의 판단과 예측 결과는 사용한 데이터, 시각, 출처와 가정으로 돌아갈 수 있어야 한다.
- **데모 데이터는 내부에서 구분:** 실제 API·원본·승인 표본·시나리오 데이터·사용자 입력과 계산 주체는
  데이터 계약·fixture·adapter·기획 문서에서 구분해 실제 연계로 교체할 수 있게 한다. 기본 UI에는 이
  제작 구분을 상태 칩이나 면책 문구로 노출하지 않는다. 관측시각·갱신시각·출처기관·모델 버전처럼 실제
  제품 판단에 필요한 정보만 해당 화면의 근거로 표시한다.
- **혼합 데모:** 확보 가능한 실제 API·원본 재생·승인 표본은 쓰고, 확보하지 못한 내부 연계는 시나리오
  데이터로 채운다. 하나의 사건 기준시각을 유지하고 이벤트별 원본시각·시나리오시각·교체 대상은 내부
  데이터 계약과 02 §5.4에 기록한다. 화면은 하나의 제품 경험으로 연결하되 검증하지 않은 정확도·운영 실적을
  실제 성과로 대외 주장하지 않는다.
- **대표 데모 집중:** 모든 유형의 전체 흐름을 구현하지 않고 한 사건으로 깊이를 증명한다.
- **시연 우선 연결:** 데모는 제품 방향을 확인하는 시연이다. 화면 완성도가 아니라 기획 문서와 끊김 없이
  연결되는지를 기준으로 삼고, 합성 데이터는 같은 이벤트 계약과 사건 기준시각을 따른다.
- **수량 비목표:** 화면, 사건, 재난유형 수를 Phase 2의 성과로 삼지 않는다.

## 6. 문서 구조와 역할

활성 고도화 문서는 아래 범위로 제한한다.

| 산출물 | 역할 | 상태 |
|---|---|---|
| `README.md` | Phase 2 마스터 플랜, 순서, 결정과 진행 상태 | 대표 데모 상세 설계 정리 완료·구현 준비 |
| `01_이벤트·사건모델.md` | 수집 대상, 관측 이벤트, 사건 연결, 상태·공간·품질 모델 | 재검수 잔여항목 반영 완료·데이터 확인 필요 |
| `02_대표데모시나리오.md` | 대표 사건의 선정 근거, 입력, 시연 흐름과 완료 기준 | 재검수 잔여항목 반영 완료·P0 확인 필요 |
| `03_유형별디지털트윈.md` | 제1부 유형별 계약(입력·시간축·공간·대안·결과·준비도), 제2부 유형별 표현(장면·데이터 역할·인셋·보조뷰·부품·검수) | 2026-09-15 제2부 통합 · 계산 주체 확인 필요 |
| `04_사건작업공간_화면상세.md` | 사건 범위별 지도·관련 이벤트·판단·전망·대응 실행의 화면 연결 | 2026-09-14 화면 방향 기준안 |
| `CUVIA_NDMS_기능및정보구조도.md` | 화면 구성·화면 역할·정보 객체·대표 데모 동선 | 재검수 잔여항목 반영·엑셀 동기화 완료 |
| `CUVIA_NDMS_기능및정보구조도.xlsx` | IA MD를 업무 검토용 표로 옮긴 동기화본 | 2026-09-11 현재 IA MD에서 재생성 완료. MD 수정 뒤 다시 돌린다 |
| `검수/*.md` | 특정 판본의 독립 점검 기록, 정본 아님 | 대상 정본 보완 뒤 이력 보존 |
| `초안/*.md` | 자료 확인 중에만 쓰는 임시 작업메모, 정본 인용 금지 | 채택 내용은 소유 정본에 반영하고 작업 초안은 이력으로 보존 |
| `overview.html` | 아키텍처·사건 흐름·대표 시나리오·화면·공통 객체·준비도를 보는 시각화 뷰어 | 2026-09-11 현재 정본에서 재생성 완료 |

상세 문서는 각각 하나의 질문에 답한다. 조사 메모, 비교표와 임시 결론을 별도 활성 MD로 계속 만들지
않고 해당 상세 문서의 근거와 결정으로 흡수한다. IA 엑셀은 새 설계의 별도 정본이 아니며 IA MD를 표로
검토하고 Phase 1 자산의 재사용 여부를 판단할 때 사용한다.

세부 주제의 소유 문서는 다음처럼 고정한다.

| 주제 | 정본 |
|---|---|
| 이벤트 계층·사건 상태·결정·실행·결과 객체 | `01_이벤트·사건모델.md` |
| 대표 사건의 데이터 구성·D0~D8·재생/모의·완료 기준 | `02_대표데모시나리오.md` |
| 트윈 유형별 입력·전파·시간·공간·대안·불확실성·검증, 유형별 화면 표현·인셋·보조뷰·모의훈련 메뉴 | `03_유형별디지털트윈.md` |
| 사건 작업공간의 지도·관련 이벤트·판단·전망·대응 실행 화면 상세 | `04_사건작업공간_화면상세.md` |
| 화면·모의훈련 메뉴·공통 맥락·Phase 1 판정 | `CUVIA_NDMS_기능및정보구조도.md` |

### 6.1 문서 작성 운영 순서

핵심 문서는 다음 순서를 한 묶음으로 진행한다.

```text
작성 → 검수 → 검토 → 보완
```

| 단계 | 목적 | 결과물·처리 원칙 |
|---|---|---|
| 작성 | 원본과 공식 근거를 바탕으로 해당 정본의 기준안을 만든다 | 내용은 `01~03` 또는 IA MD에 작성한다 |
| 검수 | 문서가 자기 질문에 답하는지, 다른 정본·원본과 충돌하는지 독립적으로 찾는다 | `검수/`에 대상 버전과 근거가 명시된 점검 기록을 남기며 원문은 고치지 않는다 |
| 검토 | 검수 지적을 현재 문서와 다시 대조해 `채택·부분 채택·보류·기각`으로 판단한다 | 지적을 자동 수용하지 않고 판단 근거를 확인한다 |
| 보완 | 검토에서 채택된 내용만 정본에 반영하고 연관 문서의 상태를 맞춘다 | 새 보완 문서를 만들지 않고 대상 정본을 갱신한다 |

- `검수/` 문서는 제품 방향이나 요구사항의 정본이 아니라 특정 시점 문서에 대한 점검 기록이다.
- 검수 기록은 대상 파일의 판본·기준일 또는 줄 수를 명시한다. 원문이 바뀌면 남은 지적도 현재 판본에
  다시 대조한다.
- 검수와 검토 단계에서는 원문을 동시에 수정하지 않는다. 무엇을 반영할지 결정한 뒤 보완 단계에서만
  정본을 수정한다.
- 보완 후에는 README와 연관 상세 문서, IA MD, 엑셀 동기화본에 파급된 상태만 확인한다.
- 검수 결과를 설명하는 별도의 `최종안·보완안·재검토안` 문서를 추가로 만들지 않는다.
- 한 묶음이 끝난 뒤 중요한 구조 변경이 있으면 같은 순서로 다시 검수한다.
- `초안/` 작업메모는 미확정 사항을 잠시 정리할 때만 허용한다. 채택한 내용은 주제 소유 정본에 옮기고,
  보류한 확인사항은 해당 정본의 `남은 확인`에 기록한 뒤 같은 작업 묶음에서 `docs/레거시/작업/`으로
  이동한다. 명시적인 삭제 요청이 없으면 초안을 삭제하지 않는다. 이동한 초안은 작업 이력일 뿐 정본의
  참고 자료나 근거로 사용하지 않는다.

### 6.2 전체 시각화 HTML 운영 원칙

`overview.html`은 흩어진 정본을 한눈에 검토하기 위한 **읽기 전용 시각화 뷰어**다.

- 제품 아키텍처, 이벤트→사건 전환, 대표 데모 D0~D8, 화면, 공통 정보 객체와 데이터 준비도를
  다이어그램과 요약으로 보여준다.
- 제품 아키텍처는 새 구성요소를 정의하는 설계가 아니라 §6 소유 문서의 관계와 01의 객체·이벤트 흐름을
  시각화한 것이다.
- 새로운 요구사항·수치·결정을 HTML에서 만들지 않는다.
- 상세 내용을 복제하지 않고 각 근거 문서와 해당 절로 이동하는 링크를 제공한다.
- HTML과 MD가 다르면 마스터 플랜과 해당 상세 MD가 우선한다.
- 별도 HTML을 계속 추가하지 않고 `overview.html` 한 파일만 유지한다.
- 상세 문서와 IA 검토가 끝난 뒤 작성하고, 최종 교차검수에 사용한다.

## 7. 작업 순서와 결정 관문

### 0단계 · 마스터 플랜 확정

- 이 README에서 목적, 근거, 산출물과 작업 순서를 확정한다.
- 루트 README와 `CLAUDE.md`가 이 문서를 Phase 2 계획으로 가리키는지 확인한다.
- 백업과 활성 문서의 역할을 분리한다.

완료 관문: 무엇을 조사하고 어떤 문서를 어떤 순서로 만들지 한 문서에서 확인할 수 있다.

### 1단계 · 원본 분석과 추가 조사

- 확보한 자사·사업·타사·협력사 원본을 목적별로 분석한다.
- 공식 표준과 공공 정책·적용 사례를 추가 조사한다.
- 사실 주장은 출처·페이지와 `확인·부분 확인·미확인`을 구분하고, 제품 설계 판단과 가정은 사실처럼
  표현하지 않는다.
- 현재 보유, 파트너 연계, Phase 2 계획과 장기 후보를 분리한다.

완료 관문: 핵심 제품 결정에 출처가 있고, 문서·대외 설명에서 근거 없는 기능을 보유 기능처럼 쓰지 않는다.

### 2단계 · 이벤트·사건 모델

- `01_이벤트·사건모델.md`를 작성한다.
- 수집원별 이벤트와 필수 메타데이터를 정의한다.
- 센서·API·VLM·Forecast를 단일·복합 조건으로 평가해 어떤 알림을 만들고 언제 사건 후보로 올리는지 정의한다.
- 관측 이벤트가 사건 후보가 되고 확인·병합·종료되는 규칙을 정의한다.
- 지점·시설·회랑·구역·전역 범위와 원인·파생 사건 관계를 정의한다.

완료 관문: 대표 사건 하나가 어떤 원천 데이터에서 만들어졌는지 역추적할 수 있다.

### 3단계 · 대표 데모 시나리오

- 후보 사건을 데이터 가용성, 설명력, 트윈 차별성, 대응 연결성으로 비교한다.
- 대표 사건 한 건을 확정하고 `02_대표데모시나리오.md`를 작성한다.
- 수집부터 사건화, 예측, 승인, 실행, 결과 기록까지 시연 흐름을 확정한다.
- 실연계·계산·시나리오 데이터는 내부 계약과 기획 문서에서 구분하고 화면에서는 동일한 제품 문법으로 표현한다.

완료 관문: 한 사건만으로 CUVIA가 수집한 이벤트를 어떻게 활용하는지 설명하고 시연할 수 있다.

### 4단계 · 유형별 디지털트윈

- 재난유형을 무작정 늘리지 않고 트윈의 계산·표현 방식이 달라지는 유형군을 먼저 정의한다.
- `03_유형별디지털트윈.md`를 작성한다.
- 유형별 대표 사건, 입력, 시간축, 공간 표현, 영향 결과와 대응 활용을 정의한다.
- 데이터나 모델이 없을 때의 대기·대체 표현도 함께 설계한다.

완료 관문: 전체 시나리오를 중복 구현하지 않아도 유형별 트윈 차이를 설명할 수 있다.

### 5단계 · IA와 데모 범위 확정

- 상세 문서의 기준안을 `CUVIA_NDMS_기능및정보구조도.md`에 업무·정보구조로 변환한다.
- 기존 화면을 유지·재편·통합·제외·보류로 판정한다.
- 각 화면이 답할 질문과 대표 시나리오의 화면 동선을 확정한다.
- 데모 구현 범위와 후속 제품 범위를 분리한다.
- IA MD 변경사항을 엑셀 동기화본에 반영하고 두 파일의 불일치를 확인한다.

완료 관문: 개발자가 README, 상세 문서 3개와 IA만으로 구현 범위와 이유를 이해할 수 있다.

### 6단계 · 대표 데모 구현 및 검증

- 확정된 대표 사건만 우선 구현한다. Phase 1 상태 엔진·팝업·트윈 페이지를 뼈대로 재사용한다.
- 화면 셸·사이드바·패널 구조는 다른 CUVIA(platform_web → IDC → secon 순)의 같은 자리를 따르고 부품은
  DS(`@ds`)를 쓴다. NDMS 만의 레이아웃을 새로 만들지 않는다. 참조 경로와 우선순위는 `CLAUDE.md` 구현 규칙이 든다.
- 모든 화면이 동일한 사건 상태와 시나리오 시각을 사용하도록 연결한다.
- 구현은 기존 단계명과 구분해 `I0~I4` 묶음으로 관리한다.

| 구현 묶음 | 범위 | 완료 기준 |
|---|---|---|
| I0 기반 | E0~E10 이벤트 표, 위험도 매트릭스·복합 알림 규칙표, 대표 사건·Forecast·모의훈련 시나리오·분석 결과·개선 항목 계약, 공통 상태 엔진, IA §5.2 기존 `scr-*` 라우트와 사이드바 | 같은 근거와 매트릭스 결과에서 알림·사건 후보가 생성되고 모든 화면이 같은 사건·Forecast·시나리오 시각을 읽는다 |
| I1 최소 주동선 | 종합상황 큐 → 사건 작업공간 → 사건 지도 디지털트윈(기준 전망과 대안 비교) → 대응 → 기록·검증의 D0~D8 한 줄기 | 사건 확인, 전망 탭에서 기준·대안 비교, 고른 대안이 조치안에 실림, 승인·실행, 종료가 처음부터 끝까지 동작하고 종료된 사건이 모의훈련 목록에 선다 |
| I2 차별 장면 | VLM 근거, 위험도 매트릭스와 4축 설명, 보고서 근거 링크, 모의훈련 한 건(§6.1) | 새 화면을 늘리지 않고 비교표 `◎`의 핵심 장면이 주동선 안에서 확인된다 |
| I3 신뢰·검증 | CAP 필드 전파문, 실패·미응답·대체조치, 반대 근거, IA §21·02 §11·라우트 예외 전체 검증 | 정상 경로뿐 아니라 실패와 예외까지 사건 기록으로 남는다 |
| I4 선택 확장 | 모의훈련의 나머지 지난 사건(§6.1), 광역 인셋·보조뷰·선 층·노드·벡터·격자 부품, G 파생 관계, 기록·AI·운영 관리 보조 구조 | 일정과 시연 효과를 보고 항목별로 선택한다. 대표 도시침수 D0~D8 완료를 막지 않는다 |

- I0의 표와 계약은 01·02·IA의 필드와 상태를 코드가 읽는 형태로 옮긴다. Phase 1 자산의 유지·재편 판단은
  IA §17을 따른다.
- I0 복합 알림 규칙표에는 입력 이벤트·Forecast, 단일/복합 조건, 시간창·공간범위·품질 조건, 알림 등급,
  반복 억제·해제, 사건 후보 생성 여부, 연결 근거, 규칙 ID·버전을 둔다. 운영 수치가 미확정이면 데모
  시나리오 조건과 실개발 교체 항목을 구분한다.
- I0 위험도 매트릭스 표에는 입력 지표, 가중치·구간·결측 처리, 위험등급, 지표별 기여도, 완화·반대 근거,
  계산시각과 규칙 버전을 둔다. 4축은 별도 최종등급이 아니라 매트릭스 결과의 심각도·긴급도·확실성·추세를
  설명한다. 확정되지 않은 가중치와 기준값은 데모 설정값으로 관리한다.
- I1의 URL과 예외 처리는 IA §5.2, D8의 예측 케이스(기록·검증) 계약은 IA §13.1을 따른다.
- 실제 API는 확보되는 대로 붙이고 나머지는 같은 계약의 시나리오 데이터로 채운다. 02 §5.4 표가 기준이다.
- 원본 자료, 대외 설명, 상세 설계와 실제 데모를 다시 대조한다.
- 예측 근거, 승인 경계, 실패·대체조치와 한계를 검증한다.

완료 관문: 기획 문서와 데모가 동일한 제품 방향과 기능 상태를 말한다.

### 6.1 모의훈련 Phase 2 범위 (2026-09-16)

기준은 §2.3이고 화면 계약은 03 §26이다. 여기서는 Phase 2에 무엇을 넣고 무엇을 비전으로 남기는지만 정한다.

**필수**

| 항목 | 내용 | 묶음 |
|---|---|---|
| 전망 탭 대안 비교 복원 | 진행 중 서항 사건. 대응 둘(배수·도로 통제) × 시작 시점 셋. 결과는 §2.3 기준 ③을 넘는 줄만. 고른 대안이 [이 전망으로 조치안 갱신]으로 조치안에 실린다 | I1 |
| 모의훈련 한 건 | 창원천 하류 범람(아래 확정안) | I2 |
| 개선 항목 한 목록 | 이력 사건 기록 창 ⑤에 출처 `검증`·`훈련`으로 모인다. 훈련 출처는 모델 축을 쓰지 않는다 | I2 |
| 산수 수치 걷기 | 진입 차량·노출 인원·대피 인원·영향 건물 동 수. 전망 탭·모의훈련·저장 기록·보고서 전부에서 걷는다. 노출 대응의 결과는 도달 전 여유 시간 하나다 | I1 |

**창원천 확정안 (2026-09-16).** 센서 임계가 SOP를 띄우고, 훈련은 그 발동과 조치 사이를 본다.

| 항목 | 발동(센서 임계) | 대안 시각 | 결과 |
|---|---|---|---|
| S1 둔치 출입통제 | 상류 수위 상승 · 둔치 잠김 예상 | 실제 14:50(즉시) | 잘한 사례로 남긴다 |
| S2 상류 저류지 방류 | 14:35 합류부 기준 초과 예측 | 알린 직후 14:35 · 문턱 앞뒤 14:55·14:45 · 실제 15:05 | 합류부 수위 · 기준 수위 도달 시각 · 범람 범위 |
| S3 하구 천변도로 통제 | 〃 | 알린 직후 14:35 · 문턱 15:10 · 실제 15:22 | 도달 전 여유 · 통제 지속 시간 |

주민 전파는 넣지 않는다(규칙 6). 조건은 실제 · 강우 +20% · 강우 +50% · 만조 중첩이고, 하구 배수펌프장 정전은 상황 부여(수치 없음)다.
세우지 않는 값은 진입 차량 · 노출 인원 · 영향 건물 동 수다.

**상황은 깊은 것 한둘과 흐름만 보는 하나로 구성한다**(2026-09-16 사용자). 상황 단계를 늘리면 만들 판이 빠르게 는다.

| 깊이 | 창원천에서의 예 | 만드는 것 |
|---|---|---|
| 깊게 ① | 강우 상향 한 단계(실증 조건 축의 총 강우량) | 분석 기준 시점마다 상황 판, 대응과의 조합 판까지 |
| 깊게 ②(여유 있으면) | 만조 중첩 | 같은 구성 |
| 흐름만 ③ | 펌프 정지 결합(유형 결합) | 판을 만들지 않고 상황 부여로만. 수치 비교 없이 그 시각에 상황이 들어오는 것까지 |

**선택 (I4)**

| 항목 | 판단 근거 |
|---|---|
| 구항 해안월류 · 서항 펌프장 정전 | 결과 경로 있음(조위 입력 · 의존 관계 규칙 · 03 §15). 같은 계약으로 시나리오 한 벌씩 |
| 무학산 산불 · 교방동 급경사지 | 경로 확인 필요. 수치 비교 없이 재현과 상황 부여만 |
| 폭염 | 대응이 바꾸는 트윈 결과가 없다. 유형 결합(폭염 + 정전)의 상황 부여 시나리오로만 |
| 종료된 서항을 모의훈련 목록에 세우기 | D8에서 넘기는 진입점 |

**Phase 2에서 빼는 것.** 비전에는 남는다(03 §26.11).

- 시나리오 편집기(흐름도·의사결정 분기)와 사건 없이 만드는 가상 시나리오
- 훈련 실행(실행·정지·종료·예약·보고, 권한관리)
- 단계별 목표 시간 대비 경과
- 훈련 후 개선 보고와 개선 조치 추적
- 승인된 개선 항목을 SOP·기준에 반영하는 화면

### 7.1 결정 관문 현황

| 단계 | 현재 상태 | 남은 관문 |
|---|---|---|
| 0 마스터 플랜 | 통과 | 진행 상태 지속 갱신 |
| 1 원본·추가 조사 | 통과, 자료 확인 병행 | 특보 인증·조위 대체 API·창원 계측 원본은 구현과 병행해 확보 |
| 2 이벤트·사건 모델 | 통과 | 외부 사건·공식 상태·SOP 필드의 실제 명세는 확보 시 반영 |
| 3 대표 데모 | 통과 (혼합 데모 확정) | P0 자료가 오면 시나리오 데이터를 같은 계약으로 교체 |
| 4 유형별 트윈 | 통과 (C·D·F, B·E·G 역할 확정) | 계산 제공 주체 대외 표기, 기관 재난분류 매핑 확인 |
| 5 IA·데모 범위 | 통과 (화면 구성·전환·엑셀·시각화 동기화) | IA 변경 시 엑셀·시각화 재생성 |
| 6 구현·검증 | 구현 묶음 I0~I4 확정, 착수 전 | I0 기획표·계약부터 진행하고 자료 확인과 병행 |

## 8. 결정 상태

### 8.1 확정된 원칙

- Phase 2는 기존 구현의 단순 확장이 아니다.
- 원본 자료와 추가 조사 결과를 기준으로 새로 설계한다.
- 이벤트 수집·사건화·활용 구조를 먼저 정의한다.
- 전체 흐름은 대표 사건 한 건으로 깊게 보여준다.
- 나머지 유형은 대표 사건과 디지털트윈의 차이를 보여준다.
- 현재 코드는 Phase 1 현황과 재사용 가능성을 확인하는 자료다.
- 백업 문서는 복구용이며 새 설계의 근거로 사용하지 않는다.
- README는 계획을 관리하고 세부 설계는 이벤트·사건, 대표 데모, 유형별 트윈과 IA 문서에서 작성한다.
- 재난유형은 행정 명칭을 나열하지 않고 계산·시간·공간·판단 방식이 다른 트윈 유형군으로 설계한다.
- 사건 확인 전 자동화는 내부 수집·정규화·분석·검토 알림으로 제한하고 외부 전파·시설 제어는 담당자가
  승인한다.
- 외부 NDMS 사건은 원본 식별과 변경 이력을 보존하며 CUVIA 사건과 자동 병합하지 않는다.
- 실제 원격제어는 Phase 2 대표 데모에서 제외하고 시설 요청·승인·결과 확인까지만 표현한다.
- VLM은 사건 근거와 감시 우선순위를 제공하며 사람의 사건 확정과 대응 승인을 대신하지 않는다.

### 8.2 확정된 데모 방향 (2026-09-10 사용자 결정 · 2026-09-14 화면 검토 반영)

- 대표 데모는 원본 근거가 가장 강한 `창원 도시침수`를 우선안으로 두고, `서항`은 실제 배수권역·센서·시설
  매핑을 확인할 우선 적용 후보로 둔다.
- 비교 유형은 해안 경계·월류, 이동 전선·확산, 지반·구조 영향을 유지한다. 기반시설 장애는 독립 업무가
  필요할 때 별도 사건으로 만들며 다른 사건의 `악화` 또는 `파생` 관계로 공통 적용한다.
- 대표 데모는 `혼합 데모`를 기본으로 하며 실제 API·원본·승인 표본을 우선 사용하고 미확보 내부 연계만
  같은 이벤트 계약의 시나리오 데이터로 보완한다.
- 2026-09-15 갱신. 유형은 디지털트윈 메뉴에서 고르고 일곱 유형이 모두 선다(03 §5). A 는 대표 데모, B·C 는 조건 세트(사전 작성 예측판으로
  시간 조절·대안 비교), D·F·G 는 개념 장면(시간·대안은 동작하되 계산이 아님), E 는 개념 예시다. 계산 주체와 실제 모델 교체
  대상은 내부에서 관리하고 준비도는 03 §15 를 따른다. 앞선 "C·D·F 유형 전환 · D·F 정적 개념 예시 · B 확장 설계 · E 장기 후보" 기준을 대체한다.
  B 유지·G 자리·E 격자 장면은 2026-09-16 C~G 를 지난 사건으로 세우며 닫았다(03 §5·§18). 2026-09-16 `/scr-05`를 모의훈련으로 재편했으므로
  유형별로 무엇을 보일지는 §2.3 기준으로 Phase 2 범위에서 다시 정한다.
- IA-02는 Phase 1 지구 재난관제를 범위가 유동적인 지도 중심 사건 작업공간으로 재편한다. 좌측은 관련
  이벤트, 우측은 판단·전망과 대응 결과 요약을 보여준다. **대응 승인·실행은 별도의 집중 팝업으로 분리**하고
  열린 동안 조작은 팝업 한 곳에서만 한다. 2026-09-14 화면 검토로 앞선 `우측 대응 패널 기본` 안을 대체했다.
  화면 연결과 배치 기준은 `04_사건작업공간_화면상세.md`가 소유한다. IA-05는 독립 페이지로 두고
  상시 접근 가능하되 대표 데모에서는 D8에 진입한다.
- 2026-09-14 화면 검토 결정. 대응 집중 팝업의 SOP는 CSMS SOP 대응 탭 구조(우선순위 MUST·SHOULD·MAY 그룹, 자동·수동
  두 축, 대응 흐름 4단계 `상황실 관제 · 담당기관 · 주민 전파 · 현장 조치`, 주민 전파 한 항목)를 따른다. 내부 자동 조치는
  사건 후보 생성 시 완료하고 대외 전파·시설 요청은 승인 뒤 실행한다. 긴급 시 판단·전망 없이 같은 팝업으로 진입한다. 안정
  전환은 실행 결과 도착 뒤 가능하며 필수 조치 게이트는 종료에 둔다. 디지털트윈은 IA §8대로 사건 화면 안에서 같은 지도가
  바뀌며 `/scr-05`는 `모의훈련` 메뉴다(2026-09-16, §2.3. 2026-09-15의 `디지털트윈` 메뉴 이름을 고친다).
- 2026-09-14 구역별 상태 배정. 대표 데모에서 사건 작업공간의 상태를 구역마다 하나씩 볼 수 있게 시나리오 데이터를 나눈다.
  서항은 사건 진행(D0 사전 감시 → D1~D8), 봉암은 감시 우선구역(사전 감시 알림만), 주남저수지는 단일 심각 알림(담당자
  확인 대기), 구항은 수위계 결측과 품질·대체 확인 알림, 양덕은 알림·사건 없는 평시 지구 현황, 팔용은 오탐 기록이다.
  2026-09-16 시작 화면(D0)에는 주의 이하만 둔다. 발표자가 `9`로 D1을 열 때 새로 서는 경계 이상은 서항 후보 하나여야
  하므로 주남 단일 심각 알림은 서항 후보 뒤(17:20)에 선다.
  사건도 알림도 없는 지구는 예외 안내 대신 같은 골격의 `지구 현황`(관측·이벤트만, 판단·전망·대응 잠김)으로 연다.
  나머지 지구는 Phase 1 장비 대장만 있어 열면 사건 없음 안내가 선다.
- 2026-09-14 확인 뎁스 축소. 후보는 여는 순간 검토 인수되고, 확인중의 다음 행동은 `[오탐]`과 `[사건 대응]` 둘이다.
  사건 대응은 확인과 대응 팝업 열기를 한 번에 하며 `사건 확인`을 따로 누르는 단계를 두지 않는다. 오탐은 확인창을
  거쳐 기록으로 닫고 되살리지 않는다(IA §5.2). 처리상태는 CSMS 이벤트상세와 같은 후보·확인중·대응중·종료·오탐
  다섯으로 줄인다. 확인됨은 대응중에 합치고(사람이 사건이라 판단하는 순간이 대응 시작, 자동 조치 즉시 실행, 승인 경계는
  SOP 항목마다) 안정은 대응중 안의 국면이다. 근거 줄의 [확인] 버튼과 위험↑ 표식은 없앤다. 담당자 결정은 오탐·대응 둘뿐이다.
- 2026-09-16 국면 이름 `통제`를 `상황 안정`으로 바꾼다. 화면 태그는 `안정`, 버튼은 `[안정으로 전환]`이다. 같은 팝업의 SOP
  항목(해안도로 통제·지하차도 진입 통제)과 겹쳐 "도로를 막으라"로 읽혔다. 뜻은 그대로다. 급격한 확대가 멈추고 조치가 유지되는
  감시 국면이고 종료가 아니다. 안정 중에도 관측·결과·예측 이벤트는 계속 사건에 쌓인다. 안정 중 새 이벤트로
  위험등급이 다시 오르면 시스템이 안정을 자동으로 풀고 담당자 알림을 낸다. 들어가는 것은 담당자, 풀리는 것은 시스템이다. 안정은
  승인 경계가 아니라서 자동 해제가 승인 경계를 넘지 않고, 늘어난 SOP의 대외 전파·시설 요청은 그대로 승인을 거친다(01 §7.4).
- 2026-09-14 권고를 SOP에 흡수. 담당자가 읽을 개념은 위험등급과 SOP 둘이다. 권고 카드·대응 수준(세기)은 화면에서 빼고,
  시스템이 채운 대상·시한·근거는 SOP 항목 아래 줄로 둔다. 어떤 항목이 서는지는 위험등급이 정한다(`minGrade`). 자동·수동·승인
  필요는 SOP 설정값이다. Recommendation 객체는 기록·검증에서 "시스템이 채운 값 대 승인된 값" 비교용으로 데이터에만 남는다.
- 2026-09-15 지도의 침수 전망 범위. 전망 탭에서 고른 시각의 범위는 등급 색이 아니라 물 색(primary-text) 하나로 그리고 침수심으로
  진하기만 달리한다. `영향 표현` 목록 맨 위에 `침수 전망 범위 · 시각 · 대안` 항목으로 서서 켜고 끌 수 있다. 안전지도 층(해안침수
  예상도·침수흔적도)은 기본 끔이다. 전망 범위와 겹치면 어느 것이 예측인지 안 보인다.
- 2026-09-15 전망 탭 정리(04 W3). 대안 비교표가 맨 위에 서고 열 머리가 선택 버튼이다. 시간축은 시계 시각 눈금이고 지난
  눈금은 흐리게, 지금 위치를 적는다. 근거는 기준시각·가정·불확실성 한 줄, 모델명·입력 이벤트는 툴팁. 버튼은 `판단으로`
  와 `이 전망으로 조치안 갱신`. 탭의 예측판 수 표시는 뺀다.
- 2026-09-15 재난관제 메뉴는 진행 중인 사건만 연다. 없으면 종합상황에 남고 "진행 중인 사건이 없습니다" 한 줄만 띄운다.
  알림 화면은 종합상황의 감지 카드를 눌러 담당자가 고를 때만 열린다. 메뉴는 누를 때마다 같은 곳이 열려야 하고 "가장 볼 만한
  곳"을 고르는 것은 메뉴가 할 일이 아니다. 알림 카드 문구는 담당자 말로 줄인다(왜 · 할 일 · 아직 사건이 아닙니다).
- 2026-09-14 세계 틱 자동 진행(2026-09-16 고침). 담당자가 조작하면 그 뒤 세계 틱(결과 도착·악화)은 엔진 타이머가 1.8초 뒤
  민다. 다음 담당자 칸에서 멈춘다. **대응은 승인 한 번이면 끝까지 알아서 돈다.** 승인 → 18:08 전파·해안도로 통제 결과, 마을방송
  실패는 유선 대체 중, 심각 상향과 추가 2건 착수 → 18:38 SOP 전부 완료, 경계 하향 → `[안정으로 전환]` → 물 빠짐 → `[종료 검토]`.
  대체조치(17:56)와 추가 승인(18:08, 박실장)은 담당자 결정으로 기록에 남지만 발표자가 누르지 않고 시나리오가 기록한다. 화면의
  `[대체조치]`·재승인 기능은 그대로 둔다. 주민 전파는 유선 연락이 끝나는 18:09에 닫혀 현장 대응 결과보다 뒤에 완료된다. 채널·조치마다
  한 칸씩 흘리고 대체조치·추가 승인에서 멈추던 순서는 길고 복잡했다. 경계 하향(18:38)은 안정 전환의 근거라 전환을 누르기 전
  묶음에 든다. 시연에서 `9`를 누르는 자리는 시작(d0→d1)뿐이고 나머지는 리허설 복구용이다.
- 2026-09-14 대응 중 악화 장면을 대표 데모 필수로 넣는다(02 D7). 18:02 도로수위 침수 기준 지속으로 심각 상향, SOP 항목 2개
  추가·재승인, 18:38 경계 하향 뒤 안정 국면. 어떤 항목이 서는지는 위험등급(`minGrade`)이 정하고 시작된 조치는 등급이
  내려가도 남는다. G 파생 장면 중 이 하나는 I4 선택이 아니라 I1 주동선에 든다.
- 2026-09-14 대응 팝업 골격을 CSMS·KISA 관제 팝업과 같게 맞춘다(04 W4). 머리 한 줄·좌 영상·우 400 3탭·바닥 한 줄, 닫기는 X·Esc.
- 2026-09-14 판단 탭 검수 반영. 조치안은 대응 시작 시점에 CUVIA가 기준 전망으로 먼저 채우고 전망 탭 선택은 조치안을 갱신한다.
  대응 수준 `통제`는 국면 `통제`와 겹쳐 `선제 통제`로 바꾼다(국면은 2026-09-16 `안정`으로 바꿨다). 사건 없는 지구는 링크에 alertId가 없어도 열린 알림을 기본으로
  연다. 구항·주남 알림은 시연 시작(16:45) 전에 생기게 시각을 당긴다(주남은 2026-09-16 서항 후보 뒤로 옮겼다). 액션 바 캡션은 승인 전·실패·결과 대기·결과 도착을
  읽는다. 상태 전환이 참조하는 결정은 이력에서 전환 줄에 흡수한다. 화면 문구는 담당자가 읽는 말로 쓰고 규칙 ID·버전·모델명·이벤트 ID·
  전파문 식별자 같은 내부 값은 기본 화면에 올리지 않는다(04 §2). 매트릭스 점수도 같은 부류라 등급 띠 위 위치와
  변화 한 줄로 바꾼다. SOP 항목 이름·우선순위는 대응 규정 그대로 둔다. 좌측 열 폭은 300으로 종합상황과 맞춘다.
- 2026-09-14 위험도 카드 정리. 우측 기여도 상자는 좌측 근거와 같은 이벤트를 다시 그린 중복이라 없앤다. 위험도 카드는
  등급 배지·등급 띠·판단 문장 1~3줄(왜 이 등급인가 · 무엇이 달라졌고 어디쯤인가 · 무엇을 못 믿나)로 줄이고, 완화 요인·
  반대 근거·불확실성·누락 정보 넷은 `반대로 볼 근거`·`아직 모르는 것` 둘로 접는다. 점수·4축·기여도 값은 데이터로만
  남는다. 판단에 들어간 근거는 좌측 근거의 `위험도에 쓰인 n건만` 토글이 보여준다.
- 예측 결과는 사건의 Forecast로 저장되고, E8의 `FORECAST_UPDATED`가 생성·갱신된 Forecast를 참조한다.
  큐·근거 목록·타임라인에는 갱신 이벤트가 한 줄로 서고, 누르면 연결된 Forecast를 보여주는 같은 지도의
  3D 예측 화면으로 바뀐다. 담당자가 고른 시각·대안이 대응 권고에 반영된다.
- 2026-09-16 결정(같은 날 앞선 "디지털트윈은 사건 기반 대응 What-if" 결정을 고친다 · §2.3). 사이드바 메뉴 `/scr-05`는 `모의훈련`이다.
  방향 검수에서 두 가지가 확인됐다. 그 화면의 알맹이가 제안서의 모의훈련(p.15·16)이었고, 진행 중 사건의 대응 판단(사건 작업공간의 일)과
  지난 사건 분석(훈련의 일)이 한 화면에 섞여 정의가 여러 번 바뀌었다.
  - 진행 중 사건의 대안 비교는 사건 작업공간 전망 탭으로 돌아간다. 09-15 전망 탭의 대안 비교표와 `이 전망으로 조치안 갱신`을 되살린다.
  - 모의훈련의 출발점은 지난 사건(원장)이다. 조건·대응·복합 조합으로 시나리오를 돌려 보고 발견을 개선 항목으로 남긴다.
    편집기로 가상 시나리오를 새로 쓰는 것은 비전 항목이다.
  - 결과 값은 §2.3 기준 ③의 두 시험(경로 · 트윈 결과)을 넘을 때만 세운다. 앞선 결정으로 세운 대응 효과 수치 중 산수(진입 차량·노출 인원·대피 인원·
    영향 건물 동 수)는 걷는다. 경로가 있는 수치(방류 → 수위·도달, 강우·조위·펌프 → 침수)는 사전 작성 판으로 두고 연결되면 교체한다.
  - 유지하는 것: 조합마다 사전 작성 예측판을 쓰고 실시간 물리 계산처럼 부르지 않는다(§2.2 신뢰 조건). `분석 결과 저장`·다시 열기·보고서.
    예측 대 실제 검증은 IA-05 예측 케이스(IA §13.1). 점수는 매기지 않는다. 하천범람은 창원천 과거 종료 사건이다.
  - 고치는 것: 앞선 결정의 "별도 모의훈련 모드는 두지 않는다(09-15 폐기 유지)"를 고친다. 화면 구성과 Phase 2 범위는 03 §25·§26과 IA를
    다시 쓰며 정한다. 03 §25·§26과 IA(IA-07 · 옛 IA-03M)는 2026-09-16 고쳤다. 02·04에 남은 What-if 서술과 충돌하면 §2.3이 이긴다.
- Phase 2는 신규 URL을 만들지 않고 기존 `/scr-*` 라우트를 유지한다. 사건 중심 재편은 기존 화면 안의
  정보 구조와 패널 역할을 바꾸는 것이며, 사건·Forecast 선택은 화면 상태와 기존 query 전달 방식을
  우선 사용한다. 잘못된 선택값을 임의의 최신 사건·Forecast로 자동 대체하지 않는다.
- 펌프 장애가 독립 고장 기준을 넘을 때 별도 사건으로 분리하는 관계 모델은 유지하고, 관계선 장면은
  I4에서 선택 구현한다.

C 모의 예측과 G 파생 장면은 제품 방향에는 포함하지만 대표 도시침수 D0~D8의 완료 조건에는 넣지 않는
I4 선택 범위로 확정한다. 지난 사건을 모의훈련 시나리오로 넘기는 진입점(옛 D8 TrainingScenario)의 Phase 2 포함 여부는 §2.3 기준으로 §7에서 정한다.

위 항목은 구현 범위의 기준이다. P0 자료가 확보되면 값과 데이터만 같은 이벤트 계약으로 교체하고 방향은
바꾸지 않는다. 대외적으로 보유·실증 기능이라고 설명할 때는 §4.1의 구분을 따른다.

### 8.3 혼동하지 않을 개념

| 용어 | 의미 | 이번 데모에서의 위치 |
|---|---|---|
| 시나리오 데이터 | 실제 API·원본이 없는 이벤트를 같은 계약과 사건 시각에 맞춰 만든 데모 데이터 | D0~D8에 사용하되 제작 구분은 내부에서 관리 |
| 기준 전망 | 사건의 지금(또는 당시) 상태가 이어질 때의 미래 — 디지털트윈 기준 시뮬레이션 | 사건 작업공간의 전망 탭과 트윈의 `기준`. 종료 사건에서는 실제 대응이 기준이다 |
| 대안 분석 | 기준 전망에서 조건이나 대응을 바꿨을 때의 미래. 결과 값은 §2.3 기준 ③의 근거가 있을 때만 선다 | 진행 중 사건은 사건 작업공간 전망 탭, 지난 사건은 모의훈련(§2.3) |
| 모의훈련 시나리오 | 지난 사건(원장)에서 출발해 조건·대응·복합 조합을 정한 훈련 한 벌 | 모의훈련(`/scr-05`). `시나리오 데이터`(제작 재료)와 다른 말이다 |
| 분석 결과 | 모의훈련에서 돌려 본 한 건의 저장 단위: 사건 · 분석 기준 시점 · 기준 전망 · 바꾼 조건·대응 · 결과 · 메모 | 모의훈련 `저장된 분석`. 다시 열기·보고서. 발견은 개선 항목으로 넘기고 SOP에 바로 반영하지 않는다 |
| 예측 케이스 | 사건 하나의 예측판 + 입력 상태 + 기준·대안 전망 + 실제 대응 + 실제 결과 + 오차 | 기록·검증(IA-05)이 쌓는 단위. 사건이 끝난 뒤 예측 정확도와 대응 근거의 축적 |
| 개선 항목 | 예측 검증과 모의훈련이 찾은 고칠 거리. 축은 임계치·모델·데이터·SOP, 출처는 `검증`·`훈련` | 한 목록. 훈련 출처는 모델 축을 쓰지 않는다. 사람이 승인한 뒤 반영한다(§2.3) |

시나리오 데이터는 구현 재료, 기준 전망과 대안 분석은 트윈의 두 결과, 모의훈련 시나리오는 훈련의 단위, 분석 결과는 그 저장 단위,
예측 케이스는 검증의 단위, 개선 항목은 검증과 훈련이 함께 쓰는 고칠 거리 목록이다. 용어를 서로 바꾸어 쓰지 않으며, 첫 번째 항목은
제품 UI의 사용자 문구가 아니다. 대안 분석은 "기준 대응 대 대안 대응"이고 예측 케이스는 "예측 대 실제"다. 둘을 한 화면에 섞지 않는다.
모의훈련은 `/scr-05`의 업무다(2026-09-16, §2.3). 2026-09-15의 "모의훈련이라는 별도 업무 과정은 두지 않는다"를 고친다.

이 문서에서 `P0`는 대표 사건과 시연 방식 자체를 결정하는 필수 자료 확인, `P1`은 시연의 구체성과
신뢰도를 높이는 확인, `P2`는 사후검증 강화 자료를 뜻한다. `검토`는 검수 지적의 채택 여부를 판정하는
단계이고, `사용자 결정`은 제품 방향·화면 방식처럼 문서만으로 확정할 수 없는 선택을 뜻한다.

## 9. IA 재편 기준안

Phase 2 IA 기준안은 [`CUVIA_NDMS_기능및정보구조도.md`](./CUVIA_NDMS_기능및정보구조도.md)에서
관리한다. 이 문서에는 다음 상위 원칙만 유지한다.

- 담당자의 질문을 기준으로 `종합상황 → 사건 작업공간(디지털트윈 포함) → 대응·실행 → 기록·검증`의 업무
  공간과 `모의훈련` 메뉴를 연결한다(§2.3).
- AI는 공통 근거 탐색·요약 보조로 두고, 보고서는 사건 기록에서 생성되는 산출물로 둔다.
- 운영 관리는 제품 구조에 포함하되 대표 데모 주동선과 분리한다.
- 기존 화면은 수량으로 승계하지 않고 `유지·재편·통합·제외·보류`로 판정한다.
- 화면·패널 수가 아니라 대표 사건의 판단과 행동이 끊김 없이 이어지는지를 완료 기준으로 사용한다.
- 사건 작업공간은 한 현장을 뜻하지 않는다. 같은 원인·영향·대응으로 관리할 업무 범위를 지점·시설·구역·
  지구·도시 전역까지 수용하고, 독립 대응이 필요한 지역·시설은 연관 사건으로 분리한다.

`CUVIA_NDMS_기능및정보구조도.xlsx`는 위 IA MD를 10개 시트로 옮긴 검토용 동기화본이다. 별도의 결정을
추가하지 않으며 값이 다르면 MD가 우선한다. `Phase1판정` 시트만 기존 화면의 재사용·재편 판단을 담는다.

## 10. 외부 확인이 남은 항목

제품 방향과 화면 방식은 §8.2로 닫혔다. 아래는 기관·협력사·공식 자료에서 받아야 닫히는 것이고, 구현을
막지 않는다. 확보되면 시나리오 데이터와 내부 연계 구성을 실제 데이터·모델로 교체한다.

- 대표 데모의 실제 서항 배수권역·센서·CCTV·펌프 매핑과 서항 적용 여부
- 동일 사건·기준시각의 관측·시설·영상·예측 원본 확보 범위와 혼합 데모에서 실제·시나리오로 구성할 항목
- 도시침수 기준·대안 예측판의 실제 제공 여부와 계산 주체의 대외 표기(자사 보유 / 협력사 제공)
- 각 유형군의 실제 데이터·모델 준비도와 대상 기관 재난분류 매핑
- 사건 후보를 자동 생성·연결할 세부 임계치·시간창·공간 기준
- IA-05 기록·검증의 실제 권한과 담당 조직. 이번 데모에서는 정하지 않고 추후 제품 개발 시 정의
- 유형별 실데이터 후보의 현행 명세(2026-09-15 초안 조사, 구현 시 재확인): 기상청 단기·초단기예보 격자 강수·기온·풍향·풍속, AWS 1분 관측,
  레이더 강수영역, K-water 우량·수위 관측소, 홍수통제소 표준수문DB(수위·유량·댐·보·홍수특보), 기상청 해양기상 부이(풍향·풍속·파고·파주기·파향),
  국립해양조사원 조위·조류(기존 Open API 35종이 2026년 제공 중단·대체된 것을 2026-09-15 사용자가 확인 → 현행 국가중점데이터 명세로 연결), 행정안전부 무더위쉼터(위치·면적·이용가능인원·냉방설비 공개 확인),
  기상청 기상특보, 산림청 산사태정보시스템·산사태위험지도(10 m 격자 위험등급, 공공데이터포털 확인), 전력거래소 전력수급현황(광역 맥락 용도만), NASA FIRMS 위성 활성화재 관측점(수 시간 지연 · 열이상 탐지), CAMS 광역 대기자료.
  K-water 수위·유량·우량은 10분/30분/시간 단위 OpenAPI 제공 확인(2026-09-15).
  유형별로 무엇을 실데이터로 쓰고 무엇을 시나리오로 만드는지는 `03_유형별디지털트윈.md` §22 표가 든다

확인되지 않은 값을 실제 사실처럼 문서에 적지 않는다. 확인되면 이 README의 상태와 02·03·IA를 갱신한다.

## 11. 바로 다음 작업

- 완료: `01_이벤트·사건모델.md` 작성과 독립 검수
- 완료: `검수/01_이벤트·사건모델_검수_2026-09-10.md`의 지적을 현재 문서와 대조 검토
- 완료: 검토에서 채택한 지적을 `01_이벤트·사건모델.md`에 보완
- 완료: `02_대표데모시나리오.md`와 완료보고서·자사 기획자료 대조, 당시 P0 세 항목 `부분 확인` 판정
- 완료: `검수/02_대표데모시나리오_검수_2026-09-10.md`를 대조하고 채택한 지적을 본문에 보완
- 완료: `검수/03_유형별디지털트윈_검수_2026-09-10.md`를 현재 정본과 대조하고 채택한 지적을 보완
- 완료: 01·02·03 백지 재검수의 신규 지적을 대조하고 채택한 계층·시연·준비도 문제를 보완
- 완료: `검수/README_검수_2026-09-10.md`와 `검수/IA_기능및정보구조도_검수_2026-09-10.md` 검토·보완
- 완료: `초안/검수반영_확인및결정_2026-09-10.md`의 재검수 잔여항목을 대조해 정본에 보완
- 완료: 화면 면책 배지 제거, 비교표 차별점·CAP 전파문·훈련 진입점·구현 순서를 01·02·IA·README에 반영
- 완료: 디지털트윈 방향(예측 이벤트·사건 지도 디지털트윈 화면·모의훈련 메뉴·C 시나리오 Forecast 기반 예측 화면·G 파생·D 산불)을 정본에 반영
- 완료: `초안/`의 API 재고·이벤트별 정의·디지털트윈 방향·화면별 판단을 소유 정본에 대조·흡수하고
  `docs/레거시/작업/`으로 이동
- 완료: 후속 이벤트 정의(E7·E0·E3·E9·E10)를 02·IA와 대조하고 훈련 전달 계약의 사건 조건·판단 자극에
  반영한 뒤 `docs/레거시/작업/이벤트별_정의_E7이후_2026-09-11.md`로 이동
- 완료: 구현 작업 계획을 I0~I4로 검토하고 C 모의 예측·G 파생 장면을 I4 선택 범위로 확정해 §7과 IA에
  흡수한 뒤 작업 초안을 `docs/레거시/작업/`으로 이동
- 완료: `초안/README.md`에 초안 폴더 규칙 작성. 구현 작업 계획 초안은 `레거시/작업`으로 이동
- 완료(2026-09-15): scr-05를 `디지털트윈` 메뉴로 재편(유형·지역·조건 세트, D8 스냅샷, 훈련 기록). 유형별 표현 최종안·데모데이터 표현가이드·산불 표현
  가이드 세 초안을 03 제2부로 흡수(대안의 현상/노출 구분, 보조 분석뷰, 데이터 역할 표, 검수 항목, 산불 부록)하고 03 §15·§16·§17,
  IA §4·§5.2·§13·§17, README §10에 반영. 초안은 `레거시/작업`으로 이동
- 사용자 결정 대기: B 유형의 메뉴 포함 유지, G의 자리(독립 대 A 파생 장면), E 격자 노출 장면 제작, B·C·G 첫 줄 지표 교체,
  광역 인셋·선 층·노드·벡터·격자 부품의 Phase 2 범위
- 완료(2026-09-15 저녁): 모의훈련 모드 폐기. 디지털트윈 = 조건 기반 예측 시뮬레이션, 사건 뒤 검증은 IA-05 예측 케이스로 정리(03 §17·§25, IA §4·§5.2·§13.1, CLAUDE.md)
- 완료(2026-09-16): 하천범람(B) 지역을 주남저수지에서 창원천으로 옮김. 베이스맵에서 창원천 물길(명곡동 부근 → 남천 합류 → 마산만 하구)과 천변도로를 채록하고,
  10 m 지형에서 하구 물길부터 채우는 범람면 6단계를 구웠다. 하천 선은 관측소에서 네 구간으로 끊어 상태 색이 상류에서 하류로 번지고, 종단도는 관측소별 기준 수위를 쓴다.
  지표는 합류부 수위, 도달은 합류부 수위가 기준 2.5 m 를 넘는 시각으로 정했다. 지형 수면은 유형이 아니라 침수면 이름으로 찾는다(서항·창원천). 주남저수지는 예측판 없는 후보로 남긴다(03 §16)
- 완료(2026-09-15 저녁): 디지털트윈 메뉴를 상단 탭 둘로 세움. `새 사전 분석`은 기존 시뮬레이션이고 `저장된 분석`은 게시판형 목록 → 결과 문서 상세 →
  [시뮬레이션 다시 열기]·[보고서 생성]이다. 저장은 분석명·메모만 받고 조건·전망·비교 결과는 화면 값 그대로 담으며, 보고서는 `/scr-07?analysis=…`에서
  사건 보고서와 같은 렌더러로 서고 저장 순간의 지도 한 장이 마지막 절로 붙는다. 사전 분석은 IA-05 에 쌓지 않는다(03 §26 · IA §10 · §5.2).
  탭 줄 구조는 METIS SubNav(SCR-044·SCR-042)를, 게시판은 platform_web `features/sms-history/` 를 같은 모양으로 짜고 부품만 DS 로 바꿨다
  (표는 DS `DataTable` 기본값 그대로 — 외곽 스트로크·헤더 입체 띠·줄무늬가 거기 있다. `bordered` 를 끄지 않는다).
  보고서 문서 양식은 `KISA 월간 운영 보고서 양식 v0.5`(2026-09-08)를 정본으로 옮겼고 사건 보고서와 사전 분석 보고서가 같은 종이를 쓴다. 훈련·회차·결정 기록·SOP 는 없다
- 완료(2026-09-15 저녁): 예측 케이스 화면 구현. `/scr-04?tab=case` 예측 검증 탭이 예측 대 실제 표 · 예측 범위 대 실제 범위 지도 · 영향 대상 적중 ·
  실제 대응 · 개선 항목을 보이고 점수를 매기지 않는다. 값은 원장에서 파생하며(`selectors.predictionCasesAt`) 원 사건·예측판·결정·조치를 고치지 않는다.
  실측 침수 범위(`GEO-FLOOD-ACT`)와 대상별 실제 상태(`Outcome.verification.observedTargets`), 개선 항목의 축(임계치·모델·데이터·SOP)을 함께 세웠다
- 완료(2026-09-15 저녁): 사건 전망 탭(`/scr-02?panel=twin`) 고도화. 지형을 채운 침수면과 장면 층(도로 상태 선·차단·우회로·시설 상태)을 디지털트윈과 같은 부품으로 올리고,
  대안에 변화량 한 줄과 예측 기준 입력 접힘을 붙였다. 전망 타임라인은 옮기지 않는다(사건 안에서는 유효 시각 눈금 줄이 이미 그 일을 한다)
- 완료(2026-09-16): 디지털트윈을 사건 기반 대응 What-if 로 구현. 상단 탭은 [사건][저장된 분석]이고 사건 목록에서 진행 중(서항 복합침수)·종료(창원천 하류 범람)
  사건을 고른다. 레일은 사건 → 그대로 두면(종료: 실제로는) → 대응을 바꾸면(성격 칩 · 언제 선택지 셋 · 대상 · 결론 한 줄) → 달라지는 것(바뀐 줄만) → 근거, 바닥에 [분석 결과 저장].
  창원천은 2024-08-28 종료 사건으로 세웠다. 분석 시점 셋(범람 예측 발생 · 위험도 상향 · 천변도로 통제), 기준은 실제 대응(방류 15:05 · 천변도로 통제 15:22),
  방류 조정·도로 통제를 실제·10분 일찍·20분 일찍으로 비교한다. 분석 시점보다 이른 선택지는 닫히고, 종료 사건의 좌하단은 오늘 예보가 아니라 당시 조건 줄이다.
  사건 전망 탭은 기준 전망만 보이고(그대로 두면 카드 · 영향 대상) [디지털트윈에서 비교]로 그 사건의 트윈을 열며, 트윈 머리의 [사건 작업공간]으로 돌아온다.
  저장 → 목록 → 정보 창(기준 · 대응) → 보고서 창·전체 화면 → 다시 열기를 같은 분석 결과 한 벌로 확인했다. 사건 없는 조건 세트 화면과 사전 분석 형식은 걷었고,
  C~G 개념 장면은 뒤이어 지난 사건으로 옮겼다(아래)(03 §21·§26·§28, 02 D5, 04 W3, IA §5.2·§8·§13)
- 완료(2026-09-16): 진행 중 사건의 시작 시점을 `지금 기준 즉시 · 10분 뒤 · 20분 뒤`로 고침(사용자 "응 정리해줘"). 처음 정한 `즉시 · 20분 전 · 10분 전`은
  17:13 예측판 시각에 묶여 대안 비교를 여는 17:39 에 이미 지난 시각으로 보였고, 상태 줄도 사건 작업공간과 달랐다. 진행 중 사건의 분석 시점은 지금이고
  상태 줄·지난 선택지·저장 기록이 지금 시계를 따른다. 서항 대안 예측판 여섯 벌(배수·통제 × 17:40·17:50·18:00)을 다시 썼고, 20분 뒤 도로 통제(9대 · 22명)는
  실제 흐름(통제 완료 18:06)과 같은 크기다. 종료 사건(실제 · 10분 일찍 · 20분 일찍)은 그대로다(03 §26.2·§26.3)
- 완료(2026-09-16): C~G 장면을 지난 사건으로 세움(사용자 "C~G 장면(구항, 산불, 폭염 등) 순차적으로 넣어줘. 지난 사건으로 넣으면 되잖아").
  구항 방파제 월류(C · 2024-08-20) · 무학산 산불(D · 04-02) · 창원 도심 폭염(E · 08-06~07) · 교방동 급경사지 변위(F · 07-10~11) ·
  서항 펌프장 정전 파급(G · 07-24). 사건마다 주요 시점 셋 · 실제 대응 · 실제 대응 기준 예측판 · 대응 둘을 일찍 한 대안 예측판 넷을 두고,
  대응 하나는 현상 감소, 하나는 노출 감소로 갈리게 했다(E 는 둘 다 노출 감소 · 폭염은 대응으로 줄지 않는다). 느린 재난(E·F)은 1시간·2시간 일찍.
  지난 사건 지도는 오늘 자료인 기상 층을 기본으로 끄고, 도달 문구는 유형이 고른다(화선 도달 · 노출 시작 · 기능 중단). 조건 세트 준비물·형식은 지웠다.
  이로써 B 유지 · G 자리 · E 격자 장면 세 결정 대기가 닫혔다(03 §5·§16·§18·§28, IA §8)
- 완료(2026-09-16): 방향 검수. 타사 7건과 사업 원본 5건(창원 제안서 · 요약설명자료 · 24년 도시침수 완료보고 · 남해군 2건)을 원문 대조했다.
  대응을 바꾼 결과 비교, 과거 사건 재시뮬레이션, 재난 유형 결합 분석, 예측 → SOP 연결은 타사 7건 모두에 없고, 모의훈련은 자사 비교표에서
  CUVIA만 ◎다. 이에 따라 `/scr-05`를 모의훈련으로 재편하고 §2.3 모의훈련 기준을 세웠다. 같은 날 폭염을 트윈 목록에서 뺀 것(대응 효과 근거 없음)은
  §2.3 기준 ③과 같은 판단이며, 모의훈련에서 유형별로 무엇을 보일지는 Phase 2 범위에서 다시 정한다
- 완료(2026-09-16): §2.3 모의훈련 기준을 03(§2·§15~§18·§25·§26) · IA(IA-07 · §5·§5.2·§8·§10·§13·§15·§17·§18·§20·§21) · 02(D5·§5.5·차별점 표) ·
  04(판단 요약·W3 전망 탭)에 반영하고, IA 엑셀과 `overview.html`을 다시 만들었다. IA ID 를 바꾸면 `_build/build_overview.py` 가 멈추므로 생성기도 함께 고쳤다.
  Phase 2 범위는 §6.1에 정했다. 전망 탭 대안 비교 복원과 창원천 한 건이 필수이고, 상황은 깊은 것 한둘과 흐름만 보는 하나로 둔다
- 완료(2026-09-16): 모의훈련 코드 1차(창원천 기준). 메뉴·목록·시나리오 시간표(상황 부여·분기)·개선 항목·저장·보고서까지 넣고,
  산수로 만든 수치와 노출 곱셈 규칙을 걷었다. 상황을 버튼에서 시간표 위로 옮긴 것과 좌하단 조건 줄 제거는 사용자 지적이다(03 §21·§26·§28)
- 완료(2026-09-16): 모의훈련 코드 2차(창원천 기준). ① 규정 줄 — 센서 임계가 띄운 조치 셋(S1 둔치 통제 · S2 방류 · S3 천변도로 통제)과
  발동에서 조치까지 걸린 시간(15 · 30 · 47분)이 시간표에 선다. ② 대안 시각 — 임의값(10분 일찍)을 걷고 규칙으로 잡았다:
  알린 직후(발동 14:35) · 문턱(방류 14:55 · 통제는 도달 10분 전 15:12) · 실제. 예측 범위 안에 기준 수위를 안 넘으면 도달을 `없음`으로 적는다.
  ③ 규정 점검 결론 — 시간표 규정 블록 바닥("3건 모두 조치. 가장 늦은 것은 하구 천변도로 통제 47분, 여유 0분입니다")과 결과 표 바닥
  ("알린 직후 14:35에 했다면 도달 47분 전 · 천변도로 이용자 노출 감소")에 한 줄씩. 각 줄의 [개선 항목으로]가 축과 근거를 입력칸에 넘기고
  고칠 문장은 사람이 적는다. `판정` 카드를 따로 세웠다가 걷었다 — 같은 값을 두 번 말하고 실제와 대안 사이를 왕복해 읽혔다(03 §26.7·§26.8·§28)
- 완료(2026-09-16): 모의훈련 코드 3차 — **훈련 분기**(03 §26.4). 결과를 보기 전에 고르게 한다. 창원천은 14:35(범람 예측 발생 · S3 발동)에서
  잠긴 채로 열리고, 그 시각 이후 원장 · 실제 조치 시각과 지연 · 사후 관측 · 결과 표 · 개선 항목 · 저장이 모두 접힌다. 질문은
  "하구 천변도로를 지금 통제할까요?"이고 선택지는 결정 둘([지금 통제 14:35] · [수위 한 번 더 보고 15:12] · 실제 15:22 는 선택지가 아니다).
  [이 판단으로 진행]을 누르면 잠금이 완전히 풀리고 질문 자리에 `내 판단` 한 줄이 남는다. 결과 표의 열 이름과 결론 문장도 고른 결정의 이름으로 부른다.
  근거: 사용자·코덱스 지적 "결과를 처음부터 다 보여 주면 선택이 아니라 이미 아는 답을 바꿔 보는 분석이다". 위험도 매트릭스는 넣지 않았다 —
  종료 사건에 그 값이 없어 지어내야 한다. 훈련 분기가 없는 사건은 지금처럼 열린 탐색 화면이다(03 §26.4·§28, `WhatIfDrill`)
- 완료(2026-09-16): 모의훈련 위계·문구 정리(03 §26.4·§26.7). 기능은 안 더하고 자리와 말만 고쳤다.
  ① **질문이 화면의 주인공**이 된다 — 잠김에서 질문 카드가 발동 규정(질문의 근거)을 흡수해 사건 머리 바로 아래 선다. 시간표에 같은 목록을 한 번 더 세우지 않는다.
  ② **선택 뒤에는 결과가 맨 위**다 — `내 판단` → 결과 표 + 근거 두 줄(규정 지연 · 대응 효과) → 개선 항목, 그 아래 접힘 셋(다시 해 보기 · 사건 진행 · 근거).
  스크롤을 옮기는 처리가 필요 없어졌다. 풀림 스크롤 2.5장 → 1.0장, 레일 카드 8개 → 4개.
  ③ **둘째 선택지는 `15:00 재확인 뒤 통제`** — 이름에 성격을 담고, 데모가 주지 않는 갈래(확인해 보니 아니어서 취소)는 고려사항으로만 적는다.
  ④ **결론 문장이 바뀐 것과 안 바뀐 것을 가른다** — `통제 여유 0분 → 10분. 합류부 수위와 도달 시각은 그대로`.
  앞선 "도달 10분 전 · 영향 변화 없음"은 같은 말이 두 번 나오고 끝이 "변화 없음"이라 훈련의 답이 "아무것도 안 달라졌다"로 읽혔다.
  여유 표기는 `여유 n분` 하나로 통일했다(표의 대응 시점 칸과 결론 줄이 같은 함수).
  ⑤ 비교 표 가독성 — 열 이름은 `실제`·`내 판단`, 시각과 여유는 두 줄, `그대로` 목록은 결론 뒤 접힘.
  ⑥ `15:00 확인` 줄은 그 시각 원장에 있는 것만 적는다 — 상태 줄이 돌려주는 14:50 수위(1.6 m)를 "15:00 확인"으로 적어 종단도(1.9 m)와 어긋났다
- 완료(2026-09-16): 훈련 질문을 **방류**로 확정(03 §26.4). 도로 통제로 물었더니 결과가 `여유 0분 → 10분` 하나뿐이라 읽히지 않았다.
  방류는 합류부 수위(1.9 → 1.8 m) · 도달(15:22 → 15:37) · 저지대 건물(노출 → 영향 없음)이 함께 바뀌어 답이 "물이 안 넘쳤다"로 닫힌다.
  질문 카드는 묻는 규정 하나만 세우고(고를 수 없는 항목을 세우지 않는다), 잠긴 동안 `사건 진행` 접힘은 두지 않는다(질문 카드·당시 상태와 중복).
  **화면을 다듬기 전에 무엇을 물을지부터 정한다** — 이틀 동안 이 화면을 여섯 번 고친 원인이 그 순서였다.
  같은 이유로 `다시 해 보기`(상황 부여 · 분기)를 훈련 화면에서 걷었다. 훈련은 한 방향(질문 → 선택 → 결과 → 개선)이고
  자유 조합 비교는 전망 탭이 맡는다. fixture·모델 계약(`situations`·`injects`·`combos`)은 남긴다
- 완료(2026-09-16): **모의훈련 v1 폐기, v2 설계 확정.** v1(예측판 비교 + 잠금 + 질문 하나)은 벤치마킹 아홉 항목 중 셋만 담았고,
  없는 여섯이 전부 훈련을 훈련이게 만드는 것이었다. 기존 구현에서 출발한 것이 원인이다.
  v2 뼈대는 `시나리오 전개 → 규정 발동 → 조치 실행 → 결과 → 강평`이고 화면은 다섯(목록·준비·판단·결과·강평)이다.
  설계는 `검수/scr-05_모의훈련_와이어프레임_2026-09-16.html`(정적)과 `검수/scr-05_모의훈련_목업_2026-09-16.html`(클릭 가능)에 있고,
  데이터 모양 결정 11건은 `검수/scr-05_모의훈련_데이터모양_2026-09-16.html`, 검수 기록은 같은 폴더 MD에 있다.
  03 §26을 v2로 다시 썼다. **코드는 아직 v1 상태이고 다음 단계다**
- 다음(2026-09-16): 전망 탭 대안 비교 복원 · 개선 항목을 이력 예측 검증과 한 목록으로 · 나머지 지난 사건 손질. 아래는 앞선 계획이다. 순서는 ① 메뉴 이름 `모의훈련`과 지난 사건 목록 ② 산수 수치 걷기(진입 차량·노출 인원·대피 인원·건물 동 수)
  ③ 전망 탭 대안 비교 복원과 조치안 전달 ④ 창원천 시나리오 구성·상황 부여·개선 항목 ⑤ 저장·보고서에 개선 항목 절

1. 6단계 I0부터 시작한다. 이벤트 표, 위험도 매트릭스와 복합 알림 규칙표를 먼저 작성하고, 사건·Forecast·예측 케이스
   계약과 공통 상태·라우트를 코드가 읽는 구조로 만든 뒤 D0~D8 최소 주동선을 연결한다.
2. 지금 붙일 수 있는 자료를 붙인다. Open-Meteo 강수·바람·기온의 원본시각은 보존하고 시나리오 시계에
   매핑하는 adapter를 두며 하천범람지도를 연결한다. 기상청 특보 인증과 조위 대체 API를 신청한다.
   현재 판정과 공식 근거는 02 §5.3~§5.4에서 갱신한다.
3. `02_대표데모시나리오.md`의 P0~P2 확인표로 원본 로그·공간 매핑·모델 결과·SOP를 관계 기관과
   협력사에 확인한 뒤 대표 사건의 장소·기준시각과 혼합 데모 구성을 고정한다.
4. 도시침수 계산 제공 주체의 대외 표기(자사 보유 / 협력사 제공)를 원본·협력 구조로 확인한다.
5. IA MD를 고칠 때마다 `_build/build_ia_xlsx.py`로 엑셀을 다시 만들고 `build_overview.py`로 시각화를
   갱신한다. 2026-09-11 현재 두 산출물은 정본과 동기화됐다.
6. `overview.html`에 확정된 아키텍처·시나리오·화면 흐름만 다시 반영하고 정본과 교차검수한다.

대표 데모의 구현 기준과 정본·IA·엑셀·시각화 뷰어의 정리는 완료됐다. 다음 단계는 I0의 기획표와 계약부터
대표 데모 구현을 시작하는 것이다. 2~6번의 자료 확인은 구현과 병행하며, 확보되는 자료는 같은 이벤트 계약으로
교체한다. IA는 다시 확장하지 않는다.
