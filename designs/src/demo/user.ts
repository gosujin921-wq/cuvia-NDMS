/* ─────────────────────────────────────────────
 * 데모 사용자 — 재난안전 상황실 담당자 한 명
 *
 * 이 데모는 기능을 보여주는 자리라 권한 체계는 시연 대상이 아니다. 역할 전환과
 * 역할별 화면을 두지 않고, 화면은 상황실 담당자 구성 하나로 고정한다.
 * 여기 남는 것은 사이드바 프로필 표기뿐이다.
 *
 * 필드 구성은 DS AppSidebar 의 SidebarUser 를 그대로 따른다(name·group·role·lastLogin).
 * 소속 외의 값은 정본 문서에 없는 시연용 채움이다 — 실데이터가 오면 교체한다.
 * ───────────────────────────────────────────── */

export const DEMO_USER = {
  /** 데모 사용자명 */
  name: "김상황",
  /** 소속 */
  group: "재난안전 상황실",
  /** 직무 */
  role: "상황 담당",
  /** 마지막 접속 — 시연 기준 시각(DEMO_NOW 2026-08-12 17:44) 당일 주간 교대 시각 */
  lastLogin: "2026-08-12 08:30",
} as const;
