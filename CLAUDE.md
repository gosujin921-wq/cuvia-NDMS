# CUVIA NDMS 프로젝트 작업 지침

## ★ 현재 작업 단계와 정본

현재 `designs/src`는 Phase 1 프로토타입이다. 구조와 자산을 재사용할 수 있지만 제품 방향의 정답은
아니다. Phase 2에서는 기존 구조를 유지한 채 내부 정보구조를 재편하거나, 검토 결과에 따라 화면·상태·
데이터 모델을 다시 구성할 수 있다.

Phase 2 작업 문서는 다음 역할로 나눈다.

- `docs/고도화/README.md`: 목표·근거 원칙·작업 순서·결정 상태를 관리하는 마스터 플랜
- `docs/고도화/01_이벤트·사건모델.md`: 수집 이벤트와 사건 생성·연결·상태
- `docs/고도화/02_대표데모시나리오.md`: 대표 사건의 데이터·시연 흐름·완료 기준
- `docs/고도화/03_유형별디지털트윈.md`: 제1부 트윈 유형별 계약, 제2부 유형별 화면 표현·인셋·보조뷰·모의훈련 메뉴
- `docs/고도화/CUVIA_NDMS_기능및정보구조도.md`: 업무 공간·정보 객체·화면 동선의 IA 정본
- 같은 이름의 `.xlsx`: IA MD를 표로 확인하기 위한 동기화본. 값이 다르면 MD가 우선

작업 판단의 우선순위는 다음과 같다.

1. 사용자의 최신 지시
2. Phase 2 마스터 플랜과 해당 상세 문서
3. 사업·실증 원본과 공식 표준·공공 근거
4. 자사 원본, 타사·협력사 원본
5. 현재 코드와 Phase 1 문서

재난유형·사건 수·화면을 임의로 확장하거나 현재 코드를 근거로 Phase 2 방향을 축소하지 않는다. 계획과
구현 상태를 구분한다. 현재 단계는 `대표 데모 상세 설계 정리 완료·I0 구현 착수 전`이다. 구현 순서는
README §7의 I0~I4를 따르며 정본·IA·IA 엑셀·시각화 뷰어는 함께 동기화한다.

Phase 2 데모는 영업용 비전 제시, 기획·디자인 검증, 후속 실개발의 출발점을 함께 맡는다. 실제 API·원본·
승인 표본은 가능한 만큼 사용하고 미확보 연계는 같은 계약의 시나리오 데이터로 구성한다. `dataOrigin`,
`ingestionMode`, 원본시각·시나리오시각, 계산 주체와 실개발 교체 대상은 fixture·adapter·기획 문서에서
내부 관리한다. 기본 제품 UI에는 `가상`, `더미`, `계산 결과 아님` 같은 제작 구분이나 면책 문구를 노출하지
않고 관측·갱신시각, 출처기관, 모델·버전, 데이터 품질처럼 실제 사용자 판단에 필요한 정보만 표시한다.

Phase 2의 최우선 제품 흐름은 `센서·시설·API·VLM 수집 → 공통 이벤트 정규화 → 시간·공간·의미·품질 결합
→ 다지표 가중 위험도 매트릭스 판단과 Forecast 영향 반영 → 복합 알림 → 사건 후보·담당자 확인 →
디지털트윈 기준/대안 전망 → SOP 권고·승인·
실행 → 결과·보고서`다. 개별 기능과 화면은 이 흐름을 증명하기 위해 존재한다. `AttentionAlert`는 외부
특보나 대외 전파문이 아니라 Event·Forecast의 단일·복합 조건으로 만드는 내부 담당자 알림이다. 복합
알림과 사건 후보는 자동 생성할 수 있지만 사건 확정·대외 전파·시설 제어·도시 대응단계 변경은 승인
경계를 따른다.

위험도 매트릭스는 제품 자료의 수위·강우·영상 맥락 가중 결합을 기준으로 시설·Forecast·품질을 함께
평가한다. 정확한 가중치·등급 경계는 I0에서 데모 설정값과 실개발 교체값으로 구분한다. 4축은 매트릭스와
경쟁하는 별도 등급이 아니라 결과의 심각도·긴급도·확실성·추세를 설명한다. 매트릭스 결과는 알림·SOP
권고를 갱신할 수 있지만 사건 확정이나 도시 대응단계를 자동 결정하지 않는다.

디지털트윈은 독립 페이지가 아니라 사건 작업공간에서 예측 갱신 이벤트가 참조한 Forecast를 열어 같은
지도를 미래 상태로 바꾸는 화면이다. 대표 데모는 도시침수 한 사건의 D0~D8을 우선한다. B~G 지난 사건(2026-09-16 · 03 §16)은
사전 작성 예측판이고 계산 결과를 주장하지 않는다. 모의훈련에서 유형별로 무엇을 보일지는 README §2.3 기준으로 Phase 2 범위에서 다시 정한다.
사건이 끝난 뒤의 예측 대 실제 비교는 IA-05 기록·검증의 예측 케이스가 맡는다.

Phase 2 라우트는 IA §5.2를 따른다. 신규 라우트를 만들지 않고 기존 `/scr-01`·`/scr-02`·`/scr-04`·
`/scr-05`·`/scr-06`·`/scr-07`을 유지하며 화면 안의 정보 구조와 역할만 재편한다. 사건 작업공간은 현장
한 곳이 아니라 지점·시설·구역·지구·도시
전역의 유동적 범위를 수용한다. 중심 지도를 유지하고 좌측은 선택한 알림·사건의 관련 이벤트 현황, 우측은
위험 판단·전망과 대응 결과 요약을 보여준다. SOP 검토·대응 승인·실행은 별도 집중 팝업으로 분리하고
팝업이 열린 동안 활성 조작면은 하나로 둔다. 화면 상세는 `docs/고도화/04_사건작업공간_화면상세.md`를 따른다.
**차별점 기준(README §2.2 · 2026-09-16)**: "재난이 발생하기 전에 미래 영향을 예측하고, 대응을 바꿨을 때 달라지는 결과까지 비교한다."
`디지털트윈`이라는 단어·3D·단순 미래예측은 차별점이 아니다. 화면·기능은 예측 → 사건 → 전망 → 대안 분석 → 대응 판단 → 사후 검증의 어느 단계를 강하게 하는지로 판단한다.
**`/scr-05`는 `모의훈련` 메뉴다(2026-09-16 · README §2.3이 정본).** 지난 사건과 복합 조건으로 시나리오를 만들고, 조건과 대응을 바꿔
돌려 본 뒤, 발견한 것을 개선 항목으로 남겨 SOP·판단 기준·다음 훈련에 반영한다. 근거는 창원 제안서 p.15·16 "모의훈련 E-SOP 관리"와
자사 경쟁 비교표(모의훈련 CUVIA ◎, 경쟁 5사 X)다. 같은 날 앞선 "디지털트윈 = 사건 기반 대응 What-if"와 2026-09-15 "모의훈련 모드 폐기"를 고친다.
- 진행 중 사건의 대안 비교는 사건 작업공간 전망 탭이 맡는다. 대응 판단과 조치안이 거기 있다. 모의훈련으로 넘기지 않는다
- 기준 셋: ① 타사가 가진 것(SOP 시나리오 구성·단계별 응대 지침·이력 리포트·유사 사례)은 그대로 가져온다 ② CUVIA 장점은 원본 근거가 있는 것만
  ③ **결과 값은 두 시험을 넘어야 선다.** 경로: 모델 입력(실증 또는 외부 모델 수신)·공식 기준·원장 중 하나로 실제로 받을 수 있다(데모 값은
  사전 작성 판, 연결되면 교체). 트윈 결과: 도달·범위·강도·파급이다. 일찍 할수록 그만큼 줄어드는 인원·차량·건물 수 같은 산수는 데이터가 붙어도
  결과가 아니다. 못 넘으면 수치 없이 상황 부여로 둔다. 노출 대응(통제·대피)의 결과는 도달 전 여유 시간 하나다
- "학습"은 사람·SOP·판단 기준을 고치는 것이다. 시뮬레이션 결과로 예측 모델을 학습시키지 않는다(자기 출력 순환)
- 개선 항목은 예측 검증과 한 목록이다(출처 `검증`·`훈련`). 훈련 출처는 모델 축을 쓰지 않고, 사람이 승인한 뒤 반영한다
- 조합마다 사전 작성 예측판을 쓰고 실시간 물리 계산처럼 부르지 않는다. 비전 항목(편집기·복합 조합·실행·단계별 목표 시간·훈련 후 개선 보고)은
  Phase 2에서 빼도 비전에서 지우지 않는다
- 모의훈련의 IA ID 는 IA-07(옛 IA-03M)이고 화면 계약은 03 §26 이다. 02·04 에 옛 What-if 서술이 남아 있으면 README §2.3이 이긴다. 유형별 장면·광역 인셋은 `docs/고도화/03_유형별디지털트윈.md` 제2부(§20~§30)를 따른다
화면은 기획 항목을 그대로 부어 넣지 않고 **사용자가 무엇을 알고 싶은 순서**로 묶는다.
D0~D8 단계 자체는 URL이 아니라 공통 상태 엔진이 관리한다.
존재하지 않거나 병합·종료된 사건, 만료된 Forecast와 매핑할 수 없는 기존 링크는 IA §5.2의 예외 처리를
따르며 임의의 최신 객체로 조용히 대체하지 않는다.

D8의 기록·검증은 IA §13.1의 예측 케이스 계약을 따른다. 예측판·입력 상태·기준/대안 전망·실제 대응·실제 결과·오차를 한 건으로
묶되 원 사건 객체를 수정하지 않고, 작업메모의 예시 계수를 런타임 계산식으로 구현하지 않는다. 점수·평가 모델은 만들지 않는다.
예측 케이스의 개선 항목은 모의훈련과 한 목록으로 쓴다(README §2.3).

현재 목표는 전체 방향과 계획을 수립하고 대표 데모로 의도를 설명하는 것이다. 구현은 README §7의 I0부터
순서대로 진행하며 범위를 임의로 넓히지 않는다. 문서는 `README → 01~03 → IA MD → IA 엑셀`
순서로 정리하며, 같은 내용을 새 MD로 계속 분산시키지 않는다.

핵심 문서는 반드시 `작성 → 검수 → 검토 → 보완` 순서로 진행한다.

- 작성: 원본과 공식 근거를 사용해 정본에 기준안을 작성한다.
- 검수: `docs/고도화/검수/`에 특정 판본의 구조·문서 간 정합성·원본 대조 결과를 기록하며 원문은 고치지
  않는다.
- 검토: 검수 지적을 현재 판본과 다시 비교해 채택·부분 채택·보류·기각을 판단한다.
- 보완: 채택된 지적만 대상 정본에 반영한다. 별도 보완안 MD를 만들지 않는다.

검수 문서는 정본이 아니며 활성 문서의 읽기 순서에 넣지 않는다. 원문이 수정된 뒤에는 과거 검수의 줄
번호와 결론을 그대로 적용하지 말고 현재 판본에서 다시 확인한다.

`docs/고도화/초안/`은 자료 확인 중에만 쓰는 임시 작업메모다. 현재 구현이나 미확인 API를 근거로 한
내용을 Phase 2 결정으로 사용하지 않는다. 채택한 내용은 주제 소유 정본에 옮기고, 보류한 확인사항은 해당
정본의 `남은 확인`에 기록한 뒤 같은 작업 묶음에서 `docs/레거시/작업/`으로 이동한다. 사용자가 명시적으로
요청하지 않으면 초안을 삭제하지 않는다. 이동한 초안은 당시 상태를 보존하는 작업 이력이며 정본이 아니고,
현재 방향과 충돌할 수 있으므로 기본 검토 대상이나 설계 근거로 사용하지 않는다.

`docs/고도화/overview.html`은 상세 문서와 IA 검토 후 만드는 전체 시각화 뷰어다. 새 요구사항을 정의하지
않고 아키텍처·사건 흐름·대표 시나리오·화면 흐름·공통 객체·준비도와 정본 링크만 제공한다. 내용이 다르면 MD가 우선하며,
같은 목적의 HTML을 추가로 만들지 않는다.

`docs/레거시/고도화_백업/`은 복구용이다. 새 설계의 근거로 사용하거나 내용을 자동 승계하지 않는다.
`docs/레거시/작업/`은 정본에 흡수된 초안과 과거 작업 문서의 보존 위치다. 내용은 자동 승계하지 않는다.

## 작업 영역

수정 대상은 이 저장소뿐이다. 워크스페이스 사이드바에서 `▶` 가 붙은 폴더 하나만 작업 대상이고, `· ref/` 가 붙은 폴더(IDC · platform_web · secon)는 **읽기 전용 참조**다. 읽어서 가져오되 고치지 않는다. 참조 쪽 수정이 필요하면 먼저 알린다.

**Phase 1의 현재 동작을 확인할 때만 코드가 사양이다.** 확정값과 화면 동작은 `designs/src/demo/*.ts`와 화면
컴포넌트가 든다. 다만 이 규칙은 기존 프로토타입의 동작을 확인할 때만 적용한다. Phase 2의 새 지구명·
수치·문안·상태값은 먼저 Phase 2 정본에서 정의한 뒤 코드에 반영한다.

`docs/레거시/정본/01~05` 는 Phase 1 데모의 배경 자료다. 코드 주석의 `04 §11` · `03 §2` 같은 절
참조가 가리키지만 더 이상 갱신하지 않는다. Phase 1 동작과 어긋나면 코드가 맞고, Phase 2 목표와
어긋나면 `docs/고도화/README.md`의 Phase 2 계획이 맞다. `docs/` 는 `고도화/` 만 저장소에 실린다(README 참고).

## 구현 규칙 (Phase 1 유지보수 · Phase 2 I0 이후 공통)

아래 화면 구조·상태 엔진·DS·이관 규칙은 Phase 1 코드를 수정할 때와 Phase 2 구현(I0 이후)에 모두
적용한다. Phase 2의 제품 방향이나 정보구조를 결정하는 근거로는 사용하지 않는다. 무엇을 만들지는 정본이
정하고, 어떻게 만들지는 이 절이 정한다.

### 화면 구조는 다른 CUVIA 를 먼저 본다 (2026-09-11 확정)

**새 화면을 짜기 전에 워크스페이스의 `· ref/` 폴더에서 같은 자리의 구조를 먼저 찾는다.** NDMS 만의
셸·사이드바·패널 구조를 새로 발명하지 않는다. 이 데모는 제품(`cuvia_platform_web`)으로 넘어가야 하고,
제품과 구조가 다르면 이관할 때 화면을 다시 짜게 된다.

| 무엇 | 어디를 본다 |
|---|---|
| 앱 셸 · 사이드바 · 상단바 · 전체폭 레이아웃 | platform_web `packages/ui/src/app-shell.tsx` · `app-sidebar.tsx` · `app-topbar.tsx` · `full-width-layout.tsx`. NDMS `designs/src/layout/` 은 IDC `designs/src/layout/` 과 같은 모양이며 이미 이 구조를 따른다. **셸을 새로 만들지 않고 여기에 사건 중심 내비게이션만 붙인다** |
| 페이지 머리 · 좌우 패널 · 우측 상세 패널 | platform_web `features/*/components/` 의 `page-header` · `panel-layout` · `right-panel-header` 계열 |
| 앱 단위 레이아웃 조합 (하위 메뉴가 있는 화면) | platform_web `apps/metis/src/app/*-layout.tsx` · `metis-sidebar.tsx` |
| 데모 상태 · 알림 · 상단 액션 | IDC `designs/src/layout/TopbarActions.tsx` · `AlarmButton.tsx` · `state/DemoProvider.tsx` |
| 관제 전광판형 고정 비율 화면 | secon `components/layouts/ScaledLayout.tsx` |

- 순서는 **platform_web → IDC → secon** 이다. platform_web 이 제품이고 IDC 는 같은 DS 로 만든 가장 가까운
  데모다. secon 은 DS 를 안 쓰므로 구조만 보고 부품은 가져오지 않는다
- 참조 폴더는 읽기 전용이다. 파일을 복사해 오지 않고 **구조를 같은 모양으로 짜되 부품은 `@ds` 로 쓴다.**
  같은 부품이 참조 쪽에만 있고 DS 에 없으면 그 자리에 `// TODO(ds): platform_web <경로> 와 같은 부품. DS 승격 대상`
  표식을 남기고 `src/components/` 에 만든다
- 참조 세 곳이 서로 다르면 platform_web 을 따르고, platform_web 에 없는 자리는 IDC 를 따른다. 셋 다
  없는 자리만 새로 짜고 그 사실을 컴포넌트 머리 주석에 적는다
- Phase 2는 기존 `scr-*` 라우트와 같은 셸을 유지한다. IA §5.2의 역할 재편은 기존 화면·사이드바·패널을
  고쳐서 만들며 신규 라우트나 새 레이아웃을 세우지 않는다

### 데모 상태 엔진 — 단일 source of truth

**화면 컴포넌트에 시각·단계·등급·집계값을 리터럴로 쓰지 않는다.** 이 데모는 시연 도중 값이 변한다(S2 격상 · S6 승인 · S8 시계 점프). 화면마다 `"17:22"`, `"경보"`, `"3.41"` 을 하드코딩하면 격상 순간 여덟 군데(토스트·팝업·그래프·핀·뱃지·칩·시계·판정 카드)가 어긋나고, 03 §0-7 의 "모든 화면 요소가 같은 프레임에서 함께 바뀐다" 가 깨진다.

두 층으로 가른다:

- **정적 데이터** — 확정값(원장 32건 · 발령 기준 · 침수영향 · SOP 항목 · 전파 원장)은 `demo/*.ts` 가 든다. 시연 중 절대 변하지 않는다
- **시나리오 상태** — 변하는 전부를 공통 스토어 하나가 소유한다:
  - `step` (S0~S9) · `now` · `cityStage` — 화면 시계와 도시 대응단계가 여기서 파생 (04 §0)
  - `track` — 발사된 시연 트랙(서항 · 봉암). 화면 로직이 이걸 보고 갈래를 만들지 않는다(아래)
  - `selectedDeviceId` — 화면을 옮겨도 유지 (02 §2). 지구·사건은 URL 이 든다
  - `approvedResponseLevel` — [대응등급 대피로 상향] 승인이 쓴다 (04 §10)
  - `sopExecuted` · `dispatches` · `gateOverrides` — 시연 중 쌓이는 실행·전파·제어 결과

**화면에 보이는 모든 값은 `f(정적, 상태)` 로 계산한다.** 계측 단계 = §4-2 이력을 현재 시계로 자른 것, 도넛 = 원장 + 현재 시계, 핀 색 = 계측 단계, `대피 대응중` 배지 = `approvedResponseLevel`. 컴포넌트는 상태를 읽고 계산할 뿐 소유하지 않는다.

- **진행하는 길은 화면 조작 하나다.** 격상 · 권고 · 승인 · 실행 · 시계 점프는 전부 지구 클릭 · 팝업 · 트윈 · 승인 · 화면 이동이 밟고, 상태 전이는 엔진이 소유한다. 데모 컨트롤(9 키)이 하는 일은 **시간 경과**(세계 tick) 한 칸뿐이다(0 키 트랙 발사는 2026-09-16 걷었다). 같은 이벤트를 조작판에 한 벌 더 세우면 두 길이 갈라진다
- 새로고침 = S0(17:16) 리셋. 별도 리셋 수단 불필요 (04 §0)
- 선례: CUVIA_IDC `designs/src/state/DemoProvider.tsx` — 상태는 Provider 가 들고 컴포넌트는 `useDemo()` 로 읽는 구조를 그대로 따른다

### DS 컴포넌트 우선

**구조를 참조에서 가져왔으면 그 안의 부품은 DS 다.** UI 를 손으로 만들기 전에 DS(`@cuvia/components`)에 있는지 먼저 확인한다. Button · Card · Badge/StatusBadge · Tag · Dialog/Modal · DataTable · Tabs · Select · GlassPanel · MapMarker/MapControl · EmptyState · toast(sonner) 등 50여 종이 이미 있다 — 목록은 `designs/node_modules/@cuvia/components/src/index.ts` (barrel).

- import 는 항상 `@ds` (= `src/ds.ts` 재수출) 한 경로로만. `@cuvia/components` 직접 import 금지 — 제품(cuvia_platform_web) 이관 시 `ds.ts` 한 파일만 바꾸기 위한 규칙
- 직접 만드는 것은 DS 에 없는 것만 (`src/components/` 의 차트·맵 전용 부품이 그 예). 그때도 색·간격은 `@cuvia/tokens` 토큰(`var(--color-risk-lv*)` 등)을 쓰고 리터럴 색을 박지 않는다
- DS 컴포넌트가 살짝 안 맞으면 복제하지 말고 감싸서 쓴다. DS 자체를 고쳐야 하면 형제 클론에 `ds:link` 로 물려 작업 (designs/README.md)

### 이관을 전제로 짠다

이 데모는 나중에 제품(`cuvia_platform_web`)으로 넘어갈 수 있어야 한다.
**작업하면서 지킬 것은 아래 셋**이다.

#### 시연 트랙으로 갈래를 만들지 않는다

`track === "seohang"` · `event.id === HERO_EVENT_ID` 로 화면 로직을 가르면, 그 자리는 이관할 때
전부 손으로 찾아 걷어내야 한다. 지금 21곳이 있고 **유형을 하나 붙일 때마다 늘어난다.**

갈래가 필요하면 **데이터가 답을 내게 한다.** `progressionSpecOf(지구, 유형)` 이 그 본이다.
지구·유형을 받아 스펙을 찾지 트랙 이름을 보지 않는다. 새 유형·새 지구는 이 형태로 붙인다.

#### 두 벌이 된 것은 그 자리에서 위로 올린다

같은 값이 두 곳에 적히면 하나를 고칠 때 나머지가 남는다. 상수·목록은 `demo/*.ts` 가 정본이고
화면은 파생한다. 유형 목록이 `HAZARD_ORDER`, 분야 목록이 `HAZARD_FIELDS` 를 무는 것이 그 예다.

**타입만 만들고 목록을 안 만들면 화면이 손으로 다시 적게 된다.** 그러면 값을 늘릴 때
타입 검사는 통과하는데 화면에서 조용히 빠진다(2026-09-09 에 실제로 그 상태였다).

#### 값을 화면이 아니라 층에서 만든다

화면이 무는 것은 `demo/` 의 **파생 함수와 타입**이다. 원시 배열을 직접 뒤지지 않는다.
그래야 데이터 원본을 실서버로 갈 때 함수 본문만 바뀌고 화면이 안 바뀐다.

## git — 푸시는 두 곳 동시에 (2026-09-10 확정)

원격은 `github` · `gitea` 둘이고 `origin` 은 없다. "푸시하자" 는 둘 다 올리라는 뜻이다.

- `github` (백업) — main 전체. docs · CLAUDE.md · 워크스페이스 · netlify.toml 이 다 간다
- `gitea` (배포) — `designs/` 만. Gitea 쪽에서는 designs 가 루트다

```
git push github main
git push gitea "$(git subtree split --prefix=designs)":main
```

저장소는 하나다. Gitea 쪽 이력은 subtree split 이 designs 하위 커밋만 잘라 만든 것이고, split 은
결정적이라 매번 같은 커밋이 나와 fast-forward 로 이어진다. CSMS 처럼 designs 에 별도 `.git` 을 두지
않는다 — 그 구조에서는 상위 저장소가 designs 를 추적하지 못해 GitHub 에 전체를 올릴 수 없다.
